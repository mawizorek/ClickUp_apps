# Saved Views / Navigation — Smith Theatre

> Standardized saved views captured with the package (D-017). Saved views are the navigation convention for a dense master file.

---

## The plan

House-standard saved views: named layer/class visibility + zoom combos to jump to a useful state. Because classes carry object category (S-1), saved views pair naturally with class toggles. Likely Smith views, aligned to the elevation bands + departments: **deck plan** (1 DECK visible), **toe plot** (2 TOE), **catwalk rigging** (3 CATWALK + rigging classes), **all masking off**, **notes/scratch off**. List each view + what it shows.

If worth machine-comparing, a `saved-views.csv` manifest can be added (S-6).

---

## TODO (per-instance)

- [ ] Lock the standard saved-view list for Smith (name + visibility state).
- [ ] Confirm alignment with the [`layers.csv`](../standards/layers.csv) departments/elevations + the class tree.

*Canonical: [`../../VWX-BEST-PRACTICES.md`](../../VWX-BEST-PRACTICES.md) § F-012 + D-017.*
