# Saved Views / Navigation — Smith Theatre

> Standardized saved views captured with the package (D-017). Saved views are the navigation convention for moving around a dense master file.

---

## The plan

House-standard saved views: the named layer/class visibility + zoom combos that jump to a useful state. Because classes carry object category (S-1), saved views pair naturally with class toggles.

**Proposed Smith saved views (DRAFT — align to the elevation bands + class toggles):**

| Saved view | Shows |
|---|---|
| Deck Plan | `1 DECK` layers, plan view |
| Mezz Plan | `1.5 MEZZ` layers |
| Toe Pipes | `2 TOE` layers |
| Catwalk Rigging | `3 CATWALK` layers, Rigging-* classes on |
| Architecture Only | UR venue-base layers, department layers off |
| All Masking Off | global Masking-* class toggle off |

---

## TODO (per-instance)

- [ ] Confirm the saved-view list for Smith (name + visibility state).
- [ ] Confirm they align with the department/elevation layers + object classes.
- [ ] Decide if a `saved-views.csv` manifest is warranted.

*Canonical: [`../../VWX-BEST-PRACTICES.md`](../../VWX-BEST-PRACTICES.md) § F-012 + D-017.*
