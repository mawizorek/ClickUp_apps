# Changelog — Smith Theatre

> This package's version history (the plan's evolution). Distinct from [`../DECISION-LOG.md`](../DECISION-LOG.md) (cross-package decisions D-0xx). Newest at top.

---

## Format

```
## vN — YYYY-MM-DD
- what changed in the plan / standards / manifests
```

---

## v1 — 2026-07-16
- Cloned from `_TEMPLATE/` (D-018).
- `standards/layers.md` + `layers.csv`: authored the ~26-layer house list (dept × elevation) from the working Google Sheet "URITP VWX Smith Theatre BASE FILE Worksheets" (D-019). Working draft.
- `standards/datums-and-reference-planes.md`: recorded Smith's reference-plane rule — deck off interior trim face, mezz/catwalk off nominal wall (D-020, S-4). RULE only.
- `standards/sheet-layers.md`: drafted the full per-department sheet list (UR/S/L/A/R/V) from the ClickUp scheme; stays an F-016 DRAFT pending promotion (D-021).
- `standards/classes.md`: proposed the object-class tree (steel/wood/framing/masking + dash children) as a per-instance PROPOSAL, not promoted (D-022).
- `resources/`: seeded Smith categories + per-record example CSVs (`record-lighting-device.csv`, `record-rigging-point.csv`); replaced the template `record-EXAMPLE.csv` demo.
- Origin (S-3) documented, not re-derived — room-center on internal origin, already built (D-013).
