# Saved Views / Navigation

> Standardized saved views captured with the package (D-017 — promoted maybe→yes). Saved views are the navigation convention for moving around a dense master file.

---

## The plan

House-standard saved views: the named layer/class visibility + zoom combos that let a user jump to a useful state (e.g. "deck plan," "catwalk rigging," "all masking off"). Because classes carry object category (S-1), saved views pair naturally with class toggles.

List each saved view + what it shows (which layers/classes visible). If worth machine-comparing, a `saved-views.csv` manifest can be added (S-6).

---

## TODO (per-instance)

- [ ] List the standard saved views for this venue (name + visibility state).
- [ ] Confirm they align with the department/elevation layers + object classes.

*Canonical: [`../../VWX-BEST-PRACTICES.md`](../../VWX-BEST-PRACTICES.md) § F-012 + D-017.*
