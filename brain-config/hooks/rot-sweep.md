# 🚫 RETIRED · `rot-sweep.md`

**This file is a stub. The tool lives at [`brain-config/hooks/doc-rot-sweep.md`](./doc-rot-sweep.md).**

Retired **2026-07-25**, the same day it was created.

## Why

Two parallel passes wrote the same tool under two names within hours: `rot-sweep.md` and `doc-rot-sweep.md`. Both v1. Both dated 2026-07-25. Both attributed to Dev Dexter. Neither pass saw the other.

That is **Test D (two claimants on one truth) committed by the tool written to catch it.** The lesson is now tell 9 in the surviving file: *parallel work duplicates before it drifts, and both copies date themselves v1.*

## What survived

`doc-rot-sweep.md` v2 absorbed this file's claim-classification table (PRESCRIPTIVE / DESCRIPTIVE / STRUCTURAL / POINTER), its "tells" list, and its report format. Nothing was lost. The surviving name states the lane in the filename, which is the point: Recon Renata sweeps repo *shape*, this sweeps *documented claims*.

`/rot-sweep` still works. It is a registered alias on `doc-rot-sweep.md`, along with "run a rot sweep."

## Rules

- **Do not restore this file.** Repoint anything aimed here at `doc-rot-sweep.md`.
- There is **no ClickUp Skill** for this tool and there must not be one. Tools live in git only (LOCKED 2026-07-25, see `brain-config/skills-integration.md`).
