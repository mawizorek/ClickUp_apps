# Session Board

## Active

- **Brain** — rebuilding `preview.html` in `filemaker/maw-budget/layouts/utility/LAYOUT-global-variables/` as a pure, chrome-free, interactive Browse-mode render (no layout bar/ruler/status). Updating `layout.json`, README, `layouts/_index.json`, `meta/changelog.md`. Also logging the viewer decision (z-fm-layout-object-viewer at filemaker/ level). Branch `maw-budget-preview-pure-interactive`.
