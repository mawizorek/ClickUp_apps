# Session Board

## Active

- **Brain** — adding the real to-scale FileMaker render `preview.html` to `filemaker/maw-budget/layouts/utility/LAYOUT-global-variables/`, plus geometry/theme/viewName additions to `layout.json`, README + `layouts/_index.json` + `meta/changelog.md`. Branch `maw-budget-layout-global-preview`. index.html (inspector) untouched.
