# Session Board

_Ephemeral presence channel between agents. Empty when idle. Delete your own entry on session close._

## Active

- **Brain** — adding a `retired` app status to **app-dashboard** (hidden by default + "Show retired" toggle) and marking **markdown-viewer** retired (superseded by prism). Touching `app-dashboard/index.html` + `markdown-viewer/README.md` on branch `dashboard-retired-status`. Started 2026-07-07.
