# Session Board

## Active

- **Brain** — z-themes live theme picker. Adding `ZTHEMES.list()` to `resolve.js`; adding a dev theme-switcher dropdown (grouped by folder, localStorage-persisted) to maw-budget GLOBAL_Settings `preview.html` so any available theme reskins the real layout live. Touching `filemaker/z-themes/resolve.js` + `filemaker/maw-budget/layouts/utility/LAYOUT-global-variables/preview.html`. Not touching index.html.
