# Session Board

## Active

| Agent | Session | Working on | Files touched |
|---|---|---|---|
| Memory Maggie | Standing task `86ajq1137` | OMR drain (11 entries) | PREFERENCES.mirror.md, open-memory-requests.md, hooks/silent-fallback-law.md, session-board.md |
| **Dev Dexter** (Felix stewarding the handoff) | Fleet Build Queue `86ajmepcf` | **Routines Viewer defect fix — APP SOURCE.** Michael: *"ricky no longer wakes at 6am unless I trigger him. spec both and implement with dex?"* Wake-window card + the 3-week last-run parse break. | `routines/index.html` · **NEW** `routines/next-build-spec.md` · `VERSIONS.md` (routines row — currently unstamped) · `routines/schedule.md` (clearing the open-defect block once fixed). **NOT** touching the runbooks, `last-run/*` contents, `_shared/`, `roster.json`, or Maggie's drain files. |

_Delete your row on session close._

<p><br/></p>

_🚨 **THE SCHEDULER IS GONE** (2026-07-26). Nothing wakes; Ricky is invoke-only. Anything you read that assumes a wake timer is rot — flag it. **Any agent can run a routine:** read the runbook in `routines/`, follow it literally, stamp `routines/last-run/<routine>.txt`. One writer per file, never a shared log. `brain-config/data-refresh-log.json` was DELETED — ignore any note pointing at it._

<p><br/></p>

_Mira out 2026-07-26 ~9:20 PM ET — **Space 3 (URITP PRODUCTIONS) Pass-1 COMPLETE.** Closing writes landed in one PR: Anna's + Milo's + Clio's + my own bundle updates, two new warm archives, two cold archives, `usage-log.json` session 23. Earlier: PRs #548 (Milo rotation), #553 (Corey + Milo scope ruling), #554 (board row correction). **Nothing of Maggie's or Felix's touched.**_

_⚠️ **For whoever audits memory next:** three bundles rotated today (Milo twice, Anna once) and the pattern in both cases was per-list audit DETAIL being pasted into hot memory. The detail belongs in the session record; memory holds only what changes how the agent acts tomorrow. Standing lines added to both files._
