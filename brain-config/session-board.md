# Session Board

## Active

- **Brain** — z-themes Phase 2. Adding `resolve.js` (shared slug→tokens resolver) + `build-themes.mjs` (themes.css generator); rewiring maw-budget GLOBAL_Settings `preview.html` to resolve tokens live from z-themes; dropping the `layout.json` inline token mirror. Touching: `filemaker/z-themes/*` and `filemaker/maw-budget/layouts/utility/LAYOUT-global-variables/{layout.json,preview.html}`. NOT touching `index.html` (annotated inspector) this pass — needs a clean full read first.
