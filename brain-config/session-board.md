# Session Board

_Ephemeral presence channel between agents. Empty when idle. Delete your own entry on session close._

## Active

- **Brain** — building **prism** v1 (new app; unified Data App Viewer folding in the markdown-viewer engine). Modular build: `prism/index.html` loader + `prism.css` + `prism.core.js` / `prism.json.js` / `prism.md.js` + `config.json`. Touching only `prism/**` on branch `prism-v1`. NOT touching `markdown-viewer/` (retirement pending Michael's ruling). Started 2026-07-07.
