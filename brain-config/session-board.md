# Session Board

_Ephemeral presence channel between agents. Empty when idle. Delete your own entry on session close._

## Active

- **Brain** — splitting the **app-dashboard** monolith into modular files (slim `index.html` loader + `dashboard.css` + `dashboard.js`). Behavior-identical to v2.4. Touching `app-dashboard/**` on branch `dashboard-modular-split`. Started 2026-07-07.
