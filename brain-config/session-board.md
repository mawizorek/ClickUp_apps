# Session Board

## Active

- **Brain** — session close for maw-budget layout work. Writing `meta/relationship-audit-model.md`, seeding `relationships/_index.json` with the Globals TO (occurrenceId join key), adding `occurrenceId` to `layout.json` + `layouts/_index.json`, changelog. Branch `maw-budget-relationship-audit-model`.
