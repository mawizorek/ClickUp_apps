# Session Board

## Active

| Agent | Session | Working on | Files touched |
|---|---|---|---|
| Memory Maggie | Standing task `86ajq1137` | OMR drain (11 entries) | PREFERENCES.mirror.md, open-memory-requests.md, hooks/silent-fallback-law.md, session-board.md |
| Maestro Mira (+ Anna, Milo, Corey, Fiona) | Standing task `86ajknmmk` | Space 3 (URITP PRODUCTIONS) Pass-1 audit | super-agents/mainstage-milo/* (rotation + scope ruling) · super-agents/clickup-coach-corey/memory.md (cross-board pattern assignment). Both landed, PRs #548 + #553. Row CORRECTED in place: it previously read 'no further repo writes expected,' then Michael's scope ruling made a Corey write correct. NOT touching _shared/ (Dexter's split, still at the ceiling), roster.json (Felix released it), or Maggie's three drain files. Remaining audit work is ClickUp-side. |
| Fleet Felix | Fleet Build Queue 86ajmepcf | **Ricky's default_runbook redesign** — Michael wants a bare `Ricky` to TRIAGE a refresh log by timestamp and PROPOSE what is due, not run a fixed set. | **RE-CLAIMING `super-agents/roster.json`** (released it 5 min ago; his row's default_runbook + gate_strength change) · `hooks/data-refresh.md` (rewrite: triage default, cadence field, log-write step) · NEW `brain-config/data-refresh-log.json` (timestamp state store, usage-log.json shape) · `super-agents/routine-ricky/*`. NOT touching `_shared/` (Dexter's), Maggie's three drain files, or `mainstage-milo/*` + `clickup-coach-corey/*` (Mira's). |

_Delete your row on session close._
