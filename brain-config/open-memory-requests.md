# Open Memory Requests

_Public queue for memory-write candidates that an agent cannot or should not land itself. **Empty by default.** Maggie processes it on "run your thing on the open memory requests" and clears processed entries. Protocol: `brain-config/agents/memory-maggie/open-memory-request-protocol.md`._

**Two doors:**
- **DROP (any agent, mid-session):** "add that to the open memory log" → the current agent appends ONE entry below. That's it.
- **DRAIN (fresh session):** "open as Memory Maggie" → "run your thing on the open memory requests" → Maggie triages + places the whole batch, then clears it.

**How to use (any agent, Door 1):** append ONE entry under Open. Do not place it yourself, do not touch `/PREFERENCES.md`. Maggie decides where it lands.

**Placement is NOT yours to assume:** "preference" earns nothing. Deny-by-default for brain memory. Your suggested destination is a hint only. Most requests land in `brain-config/` (hooks / gates / agent profiles) or the Brain Reference Library, not in brain memory.

**Neighbors, don't mix:** this = memory-write candidates · `open-thread.md` = durable pending work · `session-board.md` = live presence.

> 🚨 **QUEUE STATE 2026-07-30: 14 open.** `/PREFERENCES.md` sits at ~1994/2000. **SIXTH consecutive close reporting that a qualified write cannot land**, and this close adds one more that clears the bar (`OMR-20260730-1`). **A memory file at capacity does not fail loudly — it fails by silently rejecting the next thing that deserved to be there**, and it has now done so six times running. The drain is not housekeeping; it is blocking qualified writes and the backlog is compounding.
>
> ⚠️ **The three blocker shapes, unchanged:** CAPACITY (needs Michael's cut — four entries) · BUNDLE CAP (needs rotation — Clio ×3, Fiona now over cap too) · UNREAD BUNDLE (needs nothing but running the load manifest properly — four entries). **Only the first requires a ruling.**
>
> 📌 **Maggie's standing cut proposal, now six closes old:** compress the **Domain Pointers** block (soft routing the AI Toolkit index already owns, plus burned-lesson detail like *"cached reads/SHAs burned 12x"* that belongs in the operating standard). Frees room for `-1`, `-2`, `OMR-20260728-1` and `OMR-20260730-1` together. **Michael's ruling, not hers.**

---

## Open

### OMR-20260730-1 · OPEN · 🔴 CLEARS THE BAR, BLOCKED ON CAPACITY
- Requested by: Maestro Mira (triaged by Memory Maggie at the Jul 30 close)
- Candidate note: **A well-formed answer is not evidence of a well-formed question, and correctness is not the same as usefulness.** Three instances in one session, 2026-07-30, consolidated because they are one rule rather than three anecdotes. (1) **Wrong EVENT:** a Decision Log question asked whether Michael codes a spend category *"when a purchase happens"* — he does not, so the answer was clean and correct, and the design built on it was wrong, because the outbound moment was the **annual budget submission**, not a purchase. **A precise question aimed at the wrong transaction produces a confident wrong design, and is worse than a vague one because the tidy reply suppresses doubt.** (2) **Past the decision:** the useful answer landed mid-session; six further rulings refined taxonomy that changed nothing about what happens next, and Michael's close was *"ya kinda lost me."* **A finding that does not change the next action is a doc note, not a chat message — stop when the decision is made.** (3) **The signal missed:** he asked for a diagram partway through *because the prose had already outrun him*; it was built, and the next reply returned to denser prose. **A request for a visual is a bandwidth signal, not a format request.**
- Requester's guess (non-binding): **brain memory** — Output Format or a new one-line clause beside the existing brevity rules. **Maggie concurs.**
- Context / why: **Passes deny-by-default on all three counts.** (1) Must-fire-every-response — it governs whether a reply gets SENT and how a question gets FRAMED, on every turn, in every domain. (2) Cross-agent — the house voice and all twelve teammates equally; no bundle owns it. (3) Not procedure — there is no routine to trigger, it is a composition and stopping discipline. ⚠️ **Related but distinct from `OMR-20260729-2`** (documentation has two layers): that one is about producing the wrong *artifact*; this is about producing the right analysis *past the point of usefulness*. **They pair well and should be reconciled at placement**, since both are species of over-delivering the meta-layer. ⚠️ Note the shape against Mira's existing 07-28 scar (*synthesis feels like the work*) — this may strengthen it rather than stand alone.

### OMR-20260729-1 · OPEN · 🔴 CLEARS THE BAR, BLOCKED ON CAPACITY
- Requested by: Maestro Mira (triaged by Memory Maggie at the Jul 29 close)
- Candidate note: **Prose over tables in authored documents, and no YAML front matter.** Michael, 2026-07-29: *"i don't like the table formatting meta data. i generally don't like tables in markdown format. should feel more like prose notes detailed and maintained well."* **The mechanical half worth carrying: GitHub renders YAML front matter AS A TABLE at the top of every `.md` file**, so a repo built on front-matter metadata opens every note with a grid before the reader reaches a word. **The generalizable reason: a table strips out everything except the cells, and the useful part of a note is usually the connective tissue a table has nowhere to put** — "earplugs and gloves are in both rooms, masks are in neither" is the sentence that saves a walk across the building and a three-column grid cannot hold it. Detailed, well-maintained prose is the default for any authored document; reach for a table only when the data is genuinely tabular with nothing to say about it.
- Requester's guess (non-binding): **brain memory** — Output Format, beside the existing markdown and code-block conventions. **Maggie concurs.**
- Context / why: **Passes deny-by-default on all three counts.** (1) Must-fire-every-response — it governs how output is COMPOSED, not what any agent knows. (2) Cross-agent — applies to the house voice and all twelve teammates equally, so no bundle is the right home. (3) Not procedure — there is no "how to write prose" routine; it is a formatting default. **Cost of not placing it, stated plainly: Michael reversed a taxonomy decision over this inside four hours of the decision being made, and the next session will repeat the mistake.** ⚠️ **Note the tension with `OMR-20260728-1`** (he pipes structured output into Prism and reads it as a table): those are not contradictory but they are adjacent, and **whoever places either should place both together and reconcile the boundary** — uniform structure for pasteable reports, prose for authored documents.

### OMR-20260729-2 · OPEN · 🔴 CLEARS THE BAR, BLOCKED ON CAPACITY
- Requested by: Maestro Mira (triaged by Memory Maggie at the Jul 29 close)
- Candidate note: **Documentation has two layers and only one is the deliverable.** The **reference** layer answers *what is true* — the layer list, the load limits, the class names — and is what someone opens the document FOR. The **governance** layer answers *how we decide and record* — formats, source-of-truth direction, decision history — and exists to SERVE the reference layer. **Shipping the governance layer alone is shipping the scaffolding and calling it the building.** The test before writing any documentation file: *would someone open this to look something up, or only to understand my process?* If the second, it is not the deliverable. Root cause when this fires: **mistaking PROVENANCE for VALUE** — optimizing an artifact to be defensible to an auditor when the reader is someone who needs to act.
- Requester's guess (non-binding): **brain memory** — Workflow Defaults or Output Format. **Maggie concurs.**
- Context / why: **Passes deny-by-default.** Fires on every documentation-authoring turn regardless of domain or agent, is not procedure (no routine to trigger), and no single bundle owns it. **Scored twice in one day in two different forms** — B14 count 3 (questions shipped into a repo file where a checkbox is inert) and B24 (six of eight migrated files were documentation about documentation while the reference content stayed behind). Same capacity blocker as `-1`.

### OMR-20260729-3 · OPEN · 🟡 BUNDLE-BOUND, BLOCKED ON AN UNREAD BUNDLE
- Requested by: Maestro Mira for Dev Dexter (triaged by Memory Maggie at the Jul 29 close)
- Candidate note: **A claim about Michael's professional domain is to be ASKED, not asserted.** On 2026-07-29, seated on a tree-architecture question, Dexter stated that *"in US theatre, electrics IS the lighting department"* and used it as evidence that Michael's own folder sketch contained a duplicate level. **Electrics is the department that owns powered systems: lighting, audio, AND video.** Michael's sketch was correct. **The sharper half: never build an argument on the premise that the user erred in their own field.** The taxonomy argument (five classification axes stacked in one path) was sound and needed no invented duplicate — the false claim was decoration on a good point, and it spent his credibility to make the argument land harder. Scored **B23**.
- Requester's guess (non-binding): agent profile — `super-agents/dev-dexter/memory.md`, as a scar
- Context / why: **Not placed because his `memory.md` was never read this session.** Dexter was seated through Mira from his `preferences.md` only; the load manifest was not fully run. **Writing to a bundle I have not read risks duplicating or contradicting what is already there.** Also worth pairing at placement: the front-matter taxonomy Dexter argued that same morning was **reversed by Michael four hours later** — the ruling honoured, but a same-day reversal of his own architecture call is context his bundle should carry. ⚠️ **2026-07-30: his `memory.md` WAS read this session** (seated through Mira on the Beta Budget build, full file). The unread-bundle blocker is cleared for Dexter; this is placeable on the next drain.

### OMR-20260729-4 · OPEN · 🟡 BUNDLE-BOUND, BLOCKED ON AN UNREAD BUNDLE
- Requested by: Memory Maggie for Closing Clio (Jul 29 close)
- Candidate note: **New trend-ledger entry.** `str_replace inserts eating adjacent headings · seen: 1 (four instances in one session) · last: 2026-07-29 · mitigation: after any structural doc insert, view the surrounding lines and confirm the neighbouring headings survived.` On 2026-07-29 four separate inserts into a ClickUp Decision Log consumed the heading of the block above or below them — J1, J3, and Q10 all lost their headings, plus one repaired mid-turn. **The consequence was not cosmetic: Q10 rendered as an unlabelled wall of text merged into the block above it, so Michael answered every other question in the log and never saw that one.** A structural edit that silently hides an open question from the user is a correctness failure, not a formatting one.
- Requester's guess (non-binding): agent profile — `super-agents/closing-clio/memory.md`, the recurring-hurdles ledger
- Context / why: **Not placed, two blockers.** (1) Her `memory.md` was not read this session. (2) It is **already at 10,310 B against a ~10KB hot cap** per `OMR-20260728-3/-4`, which have been waiting on rotation since 07-28. **Rotate her 07-26 entries to `memory/archive/` and then place all three together.** ⚠️ If this hurdle recurs it stops being a discipline note and becomes a tooling finding — a doc-edit tool that can eat an adjacent heading without erroring wants a guard, not a reminder. ⚠️ **2026-07-30: her `memory.md` WAS read this close** (she was seated to execute it). Unread-bundle blocker cleared; the CAP blocker stands, so the rotation is still the gate. **Four Clio entries now wait on one rotation.**

### OMR-20260729-5 · OPEN · 🟡 BUNDLE-BOUND, BLOCKED ON AN UNREAD BUNDLE
- Requested by: Maestro Mira for FMP Fiona (triaged by Memory Maggie at the Jul 29 close)
- Candidate note: **The RECORDS-vs-SOURCE boundary was adopted as written, on 2026-07-29, and it is Fiona's line.** *FileMaker holds documents as RECORDS — signed forms, dated acknowledgments, anything tied to a person or a production. Git holds documents as SOURCE — authored text that gets edited, diffed, rendered, re-delivered. The join is one-directional: an FMP record may carry a repo path; the repo never carries a copy of a record.* **Her three grounds are what carried it:** an FMP text field has no diff or blame history, it is unreadable by an agent (which was Michael's own stated reason for reopening the question), and it is a captive format that kills portability. **Corroborating precedent worth noting alongside it: this is the same shape as the Vectorworks S-5 ruling of 2026-07-16** — *git is the plan, the external tool is the realization, export is reconciliation* — arrived at independently, thirteen days earlier, in a different domain.
- Requester's guess (non-binding): agent profile — `super-agents/fmp-frank/memory.md` ⚠️ *(slug corrected 2026-07-30: the bundle is `fmp-frank/`, not `fmp-fiona/` — the slug is immutable and only the display name changed. A pointer into `fmp-fiona/` resolves to nothing.)*
- Context / why: a decision about her own lane that was ruled in her favour is exactly what a bundle exists to hold, and it will be cited every time the FMP/git boundary comes up again. ⚠️ **2026-07-30: her `memory.md` WAS read this session, and it is now ~10.8KB — OVER the ~10KB hot cap.** So this entry's blocker CHANGES from unread-bundle to **bundle cap**, and it joins two new Fiona candidates behind the same rotation. Her file also carries its own placement rule earned by blowing the cap four times on 07-29; respect it when rotating.

### OMR-20260729-6 · OPEN · 🟡 BUNDLE-BOUND, BLOCKED ON AN UNREAD BUNDLE
- Requested by: Memory Maggie for Maestro Mira (Jul 29 close)
- Candidate note: **Conducting scar — I shipped the governance layer of a body of work and left the reference content behind.** On 2026-07-29, asked to migrate the Vectorworks documentation into a new prose repo, I delivered eight files of which **six were documentation about documentation** (where the source of truth lives, which file format to use, a rule about what not to write down, and a dated decision archive) while the content that answers a working question — what layers, what classes, what symbols — stayed in the old repo, described in my own PR as "still to migrate." **A 29-row layer list was sitting in a CSV the whole time and I walked past it to write a file about the direction of truth.** Michael: *"i think you're missing the point of this whole procedure."* Scored **B24**. **What it changes for how I open a documentation turn: identify the reference content FIRST and ship it FIRST; governance is one line in a decisions file, never the headline and never delivered alone.**
- Requester's guess (non-binding): agent profile — `super-agents/maestro-mira/memory.md`, Scars
- Context / why: same shape as her existing 07-28 scar (*synthesis feels like the work, so the synthesizer skips the raw read*), and **arguably the same root cause one layer up** — both are about producing the meta-artifact instead of the thing. **Reconcile against that entry before placing; it may be a strengthening of it rather than a new scar.** ⚠️ **2026-07-30: her `memory.md` WAS read this session (full file, she drove it). Unread-bundle blocker CLEARED.** Placeable on the next drain — and **reconcile against `OMR-20260730-1` at the same time**, which is the third member of this same family (meta-layer over deliverable, past-the-point analysis, governance-over-reference). Three entries, one root cause: **consider one strengthened scar rather than three.** **The generalizable half is already queued separately as `OMR-20260729-2` for brain memory** — this is the personal instance, that is the rule.

### OMR-20260728-4 · OPEN · 🟡 APPROVED, BLOCKED ON BUNDLE CAP
- Requested by: Closing Clio (triaged by Memory Maggie at the Jul 28 evening close)
- Candidate note: **Michael-pattern, THIRD confirmation — a zero-strike Decision Log answer plus a governing note means the question was asked at the WRONG LAYER, not that he is deferring.** 2026-07-28, Q11 (store vs derive round-level `pole`/`fastestLap`): no boxes struck, note read *"i can kinda of believe any of these. but what's easiest to update. what's safest and most consistent? what's the right way to do it. i'm pretty apathetic cos ill just ask for what i want to see."* He was asking for the engineering call, not a preference. **Before writing a Q block: does this have a right answer I could work out? If yes, make the call, show the reasoning, make it cheap to reverse.**
- Requester's guess (non-binding): agent profile — `super-agents/closing-clio/memory.md`, Michael-patterns (bumps her count 2 → 3)
- Context / why: **Approved for placement, not placed.** Clio's `memory.md` is at **10,310 B against a ~10KB hot cap** and would go over with this plus OMR-20260728-3. Rotating her file while she is mid-close is the collision shape we keep scoring. **Rotate her 07-26 entries to `memory/archive/` first, then place both.** ⚠️ **2026-07-29: a THIRD Clio candidate now waits on the same rotation (`OMR-20260729-4`). Place all three in one pass.** ⚠️ **2026-07-30: a FOURTH (`OMR-20260730-2`). The rotation is now gating four entries and is the single highest-leverage unblock in this queue.**

### OMR-20260728-3 · OPEN · 🟡 APPROVED, BLOCKED ON BUNDLE CAP
- Requested by: Closing Clio (triaged by Memory Maggie at the Jul 28 evening close)
- Candidate note: **Capacity curve, second real data point.** Claude Opus 5, 2026-07-28, ~11 hours wall clock, 5 substantive beats, ~50 tool calls, 12 voices seated (Frank + 7 Workshop + 2 supplements + 2 primaries), 6 PRs merged. **Closing capacity: sharp — no degradation.** Recall held to the end: findings from the first hour (the `fastLap` completion, the sprint blocks) were cited correctly at close eleven hours later. Consistent with the 07-26 data point.
- Requester's guess (non-binding): agent profile — `super-agents/closing-clio/memory.md`, capacity curve
- Context / why: same cap blocker as OMR-20260728-4. Place both in the same pass after rotation. ⚠️ **2026-07-29 adds a THIRD data point to this curve:** Opus 5, ~3h50m, 15 substantive replies, ~90 tool calls, 13 voices seated, 7 PRs merged, closing capacity ~150K/200K (75%) and **sharp — every read re-fetched rather than carried, no recall degradation.** Fold it into the same entry rather than filing a fourth. ⚠️ **2026-07-30 adds a FOURTH:** Opus 5, ~10.8h wall clock, 12 substantive replies, ~55 tool calls, 9 voices seated, 1 PR merged. **Recall held** (a J6 ruling from hour 1 was correctly reversed against J7 at hour 2, and both were cited accurately at close). **But the session's defect was VERBOSITY DISCIPLINE, not recall** — the user disengaged at hour 10 with *"ya kinda lost me"* after six rulings that were individually correct and collectively unnecessary. **First data point where capacity was fine and the failure was output judgement.** Worth a separate axis on the curve: *recall* and *restraint* degrade independently.

### OMR-20260728-2 · OPEN · 🟡 APPROVED, BLOCKED ON A LIVE TWIN
- Requested by: Maestro Mira for Workhorse Wes (triaged by Memory Maggie at the Jul 28 evening close)
- Candidate note: **New trend-ledger entry for Wes.** `a plan's first queued step is a job that is already done · seen: 1 · last: 2026-07-28 · mitigation: verify step 1's premise against HEAD before letting a session execute it.` On 2026-07-28 the parked F1 handoff opened with *"Step 1 is the fastLap r1-8 backfill"* — **complete since 07-23**, and the doc that queued it described the OLD field shape, so a compliant agent would have overwritten `{time,lap}` objects with bare strings. **A handoff's first move is the least-questioned instruction in a session; it inherits all the authority of the handoff and none of the verification.**
- Requester's guess (non-binding): agent profile — `super-agents/workhorse-wes/memory.md`, the recurring-pitfalls ledger
- Context / why: **Approved for placement, not placed — a twin session is live.** Wes was seated in TWO concurrent sessions on 2026-07-28 (URITP audit `86ajknmmk` and the F1 pass `86ajr4bej`); per super-agent-base Concurrency rule 4 the audit session holds the pen on his `memory.md` and durable changes from the other serialize through here. **Place on the next drain, or let the audit session take it.** ⚠️ **2026-07-30: strongly corroborated.** This session's handoff-verification value showed up in the inverse direction — the FIRST act of the Jul 30 session was reading the plan's own prior artifacts, and three separate load-bearing claims in them were false against HEAD. **The entry's mitigation is correct and should be treated as a general handoff rule, not a Wes-specific note.**

### OMR-20260728-1 · OPEN · 🔴 BLOCKED ON CAPACITY (not on placement)
- Requested by: Fleet Felix (triaged by Memory Maggie at the Jul 28 close)
- Candidate note: **Michael pipes long structured output into Prism (the repo's JSON/Markdown viewer) and reads it as a TABLE.** His words, unprompted, 2026-07-28: *"that was incredibly satisfying to feed back into prism and read as a table."* Consequence for composition: dense structured output is not a wall of text to apologize for — it is **input for a viewer he owns**, and consistent parallel structure plus a stable field order are what make that paste render cleanly. Favour uniform, repeated shapes in long replies and reports.
- Requester's guess (non-binding): **brain memory** — and Maggie CONCURS, which is rare.
- Context / why: **This PASSES the deny-by-default test on all three counts** and I tried twice to route it elsewhere. (1) **Must-fire-every-response** — it governs how output is COMPOSED on every substantive reply, not what any one agent knows. (2) **Cross-agent** — it applies to the house voice and all twelve teammates equally, so no single bundle is the right home. (3) **Not procedure**, so the Procedure-is-a-tool gate does not catch it; there is no "how to format for Prism" routine, it is a fact about the READER that reframes a judgement call. **It is queued rather than placed solely because there are ~6 tokens of headroom and it needs ~20.** Placing it requires a cut, and a cut is Michael's ruling. **Maggie's picks for that cut, unchanged and now overdue: the Domain Pointers block** (~4 lines of soft routing the AI Toolkit index already owns — memory itself says *"soft routing lives in the index trigger table"*) **or the GitHub MCP pointer's operating detail** (*"cached reads/SHAs burned 12x"* belongs in the operating standard). Either buys room for this and several more. ⚠️ **2026-07-29: reconcile with `OMR-20260729-1` at placement** — that entry says prose over tables for authored documents, this one says uniform structure for pasteable output. Adjacent, not contradictory, but they must land together or one will read as overriding the other. ⚠️ **2026-07-30: a live instance in support of it.** Michael asked mid-session for a visual laid-out tree because prose had outrun him, and the artifact landed well; the same session then lost him by returning to dense prose. **The reader-facing half of this entry is now evidenced twice.**

### OMR-20260727-1 · OPEN
- Requested by: Maestro Mira (queued by Memory Maggie at close)
- Candidate note: When Michael lacks a mechanism for a decision, he answers with a CONSTRAINT rather than a pick — e.g. "want to keep it single source but not sure how," or leaving every box unchecked and writing a better question in the note. That is a decisive ruling, not a non-answer: treat the constraint as binding and go solve the mechanism.
- Requester's guess (non-binding): agent profile — `super-agents/maestro-mira/memory.md`
- Context / why: durable across every future orchestration turn, but it is relational reading-Michael context rather than must-fire-every-response behavior, so it fails the brain-memory test. Distinct from the already-locked Gold Standard rule 4 clause, which covers the mechanical checkbox reading; this is the broader behavioral pattern behind it. ⚠️ **2026-07-28: a THIRD instance of this shape fired (Q11), and Mira placed a related scar in her own bundle at that close. Reconcile this entry against what is now already written there before placing — it may be redundant.** ⚠️ **2026-07-29: a FOURTH instance — Q3 came back with zero strikes and the note *"i want to talk about this more before we commit anything. let's break it down and give me example use cases,"* which was a request for worked examples rather than options. Four confirmations; this is a settled pattern, not a hypothesis.** ⚠️ **2026-07-30: a FIFTH, and the clearest yet — asked whether the app should generate the deans' submission, he struck options and answered with the constraint *"report only cos sherri is the one who actually makes the version we submit,"* then immediately redirected the work: *"so review a URF0985 export for more per-transaction university tracking details."* The constraint plus a redirect, not a pick.**

### OMR-20260727-2 · OPEN
- Requested by: Maestro Mira (queued by Memory Maggie at close)
- Candidate note: Seating Counter Cole as designated opposition on a room that has already converged produces the session's strongest correction. On 2026-07-27 he caught that the Workshop kept its recommendation after the premise it rested on had been disproven by measurement.
- Requester's guess (non-binding): agent profile — `super-agents/maestro-mira/memory.md`
- Context / why: NOT procedure — `orchestration.md` step 8 already permits pulling extra voices, so nothing new needs authoring. What is durable is the evidence that it worked, which is exactly the kind of conducting precedent a bundle exists to hold. ⚠️ **2026-07-28 corroboration: Breaker Beckett did the same job unprompted on the F1 pass — he attacked Mira's own census (a nine-file finding published off six files) and was right. Same shape, different lens. Consider generalizing this entry to "seat an adversary against the SYNTHESIS, not just the artifact" rather than filing two lens-specific notes.** ⚠️ **2026-07-30: third corroboration, and this time the adversary was MICHAEL.** He pushed back on adding a spend-category field (*"is there a way to remap or restructure as to not bring in another field"*) and the pushback produced the session's best structural finding — that the header-category field was already half a ledger-account map. **When the user opposes a proposal, the opposition is often the finding; treat it as a seated adversary rather than an objection to answer.**

### OMR-20260727-3 · OPEN
- Requested by: Fleet Felix (bounced by Memory Maggie at the Jul 27 evening close)
- Candidate note: Re-check a size estimate at COMMIT time, not only at spec time. On 2026-07-27 the Routines Viewer v2 spec projected +1.2KB and the change landed at +4.7KB — roughly 4x off, pushing the file well past the 15KB split line. Estimating before writing was correct; the miss was never re-measuring once the code existed, which is exactly the moment a budget is easiest to skip.
- Requester's guess (non-binding): **unsure — genuine fork.** Either `super-agents/dev-dexter/memory.md` (a scar he carries) OR `hooks/source-size-budget-enforcer.md` (a step the tool should enforce).
- Context / why: **BOUNCED rather than placed, deliberately.** Two reasons. (1) Dexter was seated by Felix rather than driving his own session, and another agent's bundle is not mine to write into on his behalf. (2) If the rule is "re-measure before the commit," that is PROCEDURE and belongs in the Enforcer hook — writing it into a bundle would be a Constitution §2-§3 violation of exactly the shape scored as B16. **Resolve the hook-vs-bundle fork with the Enforcer open in front of you before placing this.** If it lands in the hook, it needs no memory write at all. ⚠️ **2026-07-28: a live instance of the same class — `f1-racetracks/README.md` grew 17,556 → 23,384 bytes during a pass whose whole purpose was correcting it, flagged in PR #574 rather than caught by a gate. Third data point for the write-time-check proposal.** ⚠️ **2026-07-30: fourth.** `quickfire/budget-code-derivation/index.html` shipped at **30.9KB, marginally over the ~30KB readback cap** — disclosed in the PR body rather than caught by a gate. Deliberate for a leaf reference page, but the pattern is identical: the overage was noticed at commit time by a human-in-the-loop, not by the enforcer.

### OMR-20260730-2 · OPEN · 🟡 BUNDLE-BOUND, BLOCKED ON BUNDLE CAP
- Requested by: Memory Maggie for Closing Clio (Jul 30 close)
- Candidate note: **New recurring-stale-reference entry.** `a plan's own prior artifacts describing the data · seen: 1 (three claims in one session) · last: 2026-07-30 · mitigation: open the data store before trusting any document that describes it, including one written by this same project.` On 2026-07-30 three load-bearing claims taken from the Beta Budget planning docs were contradicted by the actual ledger exports: a column asserted as the university's sort key was **empty on every row**, the operating line was asserted as a document-level property when it is **per-production**, and the "road code" the import spec calls *"the most accessible first-pass key"* is **not a field at all** — it is hand-typed free text inside a memo, in seven formats, and absent entirely on expense reports. **Four sizing estimates were also wrong from the same cause** (6 families → 2, ~20 operating lines → 5, ~90 records → ~9, 14 categories → 12). **The generalizable shape: a document written ABOUT a data store will always be closer to hand than the store, and a project's own planning docs are not exempt.**
- Requester's guess (non-binding): agent profile — `super-agents/closing-clio/memory.md`, recurring stale references
- Context / why: distinct from the existing entries in that ledger, which cover *index/registry pointers* and *prose banners mirroring queryable fields*. This is a third shape: **prose describing external data.** Same cap blocker as `OMR-20260728-3/-4` and `OMR-20260729-4` — **four Clio entries now wait on one rotation.**

---

## Entry template

```
### OMR-<YYYYMMDD>-<n> · OPEN
- Requested by: <agent>
- Candidate note: <self-contained, standalone-readable; must make sense with zero session history>
- Requester's guess (non-binding): <hook | gate | agent profile | reference doc | brain memory | unsure>
- Context / why: <one line>
```
