# Audit Anna — Activity Log

_Live project state + rolling session ledger. Newest on top._
_Hot window: the LIVE STATE block + the last ~2 closes. Older entries: `activity-log/2026-Q3.md` (Spaces 1, 2, 3 rotated there 2026-07-30)._

> 📌 **THIS FILE IS WHERE MY ONGOING PROJECTS LIVE** (Michael's ruling, 2026-07-30). `memory.md` holds patterns + preferences and nothing that can go stale in a day; **anything with a count, a status, a frontier or an owed answer belongs here.** Read the LIVE STATE block FIRST on any pickup.
>
> ⚠️ **Budget note for whoever rotates me:** `hooks/memory-rotation.md` sets a ~5KB sliding window, but that number was written for a pure session ledger. **This file now also carries a permanent LIVE STATE block, so the window applies to the ENTRIES BELOW it, not to the whole file.** Flagged rather than fudged — the hook needs the new shape written into it.

---

## ➡️ LIVE STATE — URITP top-down list/folder audit (my main project)

**Where it stands:** 🌟 **SPACES 1, 2, 3, 4 AND 5 ARE PASS-1 COMPLETE.** Space 5 is additionally **fully Confirmed** (all 12 DL questions answered).

**Next:** ➡️ **SPACE 6 — URITP Courses (`901313847910`)**, ~612 tasks / 7 folders, not yet opened. Then **SPACE 7 — URITP BETA BUDGET (`901313517644`)**, ~408 tasks / 4 folders. Both gate the 🎩 FOUR HATS brainstorm (his Audit Method J3: the brainstorms wait on COVERAGE).

**Index count — STAMPED, not remembered.** **154 rows: 138 Confirmed · 16 Documented**, verified 2026-07-30 11:25 AM ET against list `901327881037` with `is_subtask IN (true, false) AND is_closed IN (true, false)`. ⚠️ **The Jul 30 close artifact said 152 — it was 2 light**, because a close artifact is a snapshot and this file is not the tracker either. **Any count here older than the last close gets RE-QUERIED, not reused.** That is why this line carries a timestamp and its filter list.

**Machinery:** standing thread `86ajknmmk` (**reopen, never recreate**) · List Index `901327881037` · Audit Frontier Scan = the resume surface · List Audit DoD `12cwjm-76573` · `/council=uritp-audit` seats the bench · ⚠️ the Roadmap banner has drifted 3× — distrust it against the live Index. **`Confirmed` is Michael's word only.**

**🅿️ Parked — carry, do not resolve:** the **ROLES cluster** (9 rows, parked ×3; Corey + Milo owe a recommendation before he rules) · the **CRM projection-fan shape** (*"you're seeing growing pains"* — never read a verdict into it) · the **One Acts format** · **three umbrella folders' placement** · **SHOW TEMPLATE Q7** (three fields no production uses) · **calendar-surface sprawl** (6 surfaces) · the **Routines ↔ Season Planning dividing line** · **access control across 5 surfaces / 3 spaces** · **`| DEFINITIONS FOR PROGRAMS |`** (a real task at `researching` since Jan 2025) · **Space-4 Q4** — where the stalled-pilot/superseded line falls, which changes what every Space-4 flag means.

**📅 Scheduled:** the **Gen-1-going-forward** session with **Corey + Fiona** (that pair now has four queued conversations and no home).

**🔴 Owed / open on me:**
- **Three Index rows sit at `Documented` with NO Purpose set** — PROGRAMS, `| Routines | & Recurring`, URITP Meetings and Events (all Space 1); PROGRAMS is also missing Vitality, Cadence, SoT and List ID. Verified with the field loader, not the census. **The DoD gate says no purpose line = not Documented, so these three are mislabelled.** Raise with Michael; never silently revert a status.
- **Space 1's PROGRAMS page still describes PROGRAM SPRAWL as duplication.** The flag was reinstated with real evidence, but the page was never corrected to say what the evidence actually shows.
- **DoD deviation to raise:** the folder track says Vitality/Cadence/Residency/SoT are "N/A (folder)". I set Vitality anyway on 18 Space-3 folder rows. **The blanket N/A may be too broad.**
- **General Audit DoD** — my biggest open build: no documented protocol for auditing NON-list subjects (docs, builds, workflows, whole systems).
- **Audit-trail indexing gap** — no single "does a trail exist for subject X?" index beyond the List Index.
- **Closing Report graduation** — open question is whether the edit queue needs a severity column.

---

## 2026-07-30 · PICKUP, and Michael re-shaped how my memory works

- Opened as a **pickup** off the 6+7 baton. Council seated via `/council=uritp-audit`; standing thread reopened, not recreated; spine armed (a pickup is an OPEN).
- **Caught before walking anything:** my `memory.md` and Milo's both still read *"Spaces 1, 2 and 3 Pass-1 COMPLETE… Index = 59 rows"* — **two spaces and ~95 rows stale, sitting directly under a ⚠️ warning about stale counts.** The warning was maintained; the fact under it was not.
- **His ruling, and it generalizes fleet-wide:** *"their notes should be about patterns found and core preferences. that context should be in their ACTIVITY LOG so they can see what their ongoing projects are, not memory."* Executed: `memory.md` cut to patterns + preferences (12.6KB → 10.3KB), all project state moved into the LIVE STATE block above, Spaces 1–3 rotated to the Q3 archive.
- **The second-order win:** bundle cap is the #1 blocker in the OMR queue (four Clio entries, three Fiona entries waiting on rotation). **A third of my file was project tracking, eating a cap meant for durable learning.** Every "blocked on bundle cap" entry should be re-tested after the fleet re-shape.
- **Also fixed:** the session board's URITP row was **four days stale** and claimed the exact two files this session needed. A stale presence row is worse than an empty board — empty says "nobody posted," stale says "someone is here" and gets believed.
- **My own miss, logged honestly:** the first re-write of `memory.md` came out at **14KB, bigger than what I was fixing**, then two reword passes barely moved it. **Rewording is not trimming — only deleting a whole block moves a file.** Size Sally should have been seated on the write.

---

## 2026-07-27 → 07-30 · SPACES 4 AND 5 Pass-1 COMPLETE + the Confirm backlog cleared

- **Space 4 (URITP Programs)** — 9 lists walked one at a time · 14 Index rows · 9 list pages + 4 folder pages · DL J1–J6 / Q1–Q5. **True purpose: the PROOF layer, not a catalog** — policy → blank form → signed return → incident. Lifecycle state is encoded in NAMING because the space defines **zero custom fields**. His reframe: **the procedure-named lists are TRAINING LOGS, not per-occurrence logs.** ⚠️ Q4: it is **NOT live and operating** — between a stalled pilot and superseded, and where that line falls is still undecided.
- **Space 5 (URITP Inventories)** — 34 lists + 5 folders each walked individually · 40 Index rows (Sort 398–462) · 34 list pages + 5 folder pages · 8 Decision Logs, **all 12 questions answered.** Rulings: the space's job is **a reference of every FIXTURE owned or known** (J1) · it is **not sloppy, it is a relational database operated inside a task manager** (J2 — model↔unit populated 351/351 LX and 553/558 RIGGING, with button fields standing in for stored procedures) · **the two layers SEPARATE on migration** (J3) — ~180 model records stay in ClickUp, ~1,050 unit records go to FileMaker · **four data layers, not two:** MODEL · UNIT · CONTAINER (`STORAGE BINS`) · ADDRESS SPACE (`SPAC Relays`). Also: fungibility applies downward and retroactively (collapse ~380 identical rigging rows) · the `OWNER` field beats the name prefixes · `[90XX]` was invented here and extends to all six departments · the archive was **accidental** → unarchive · `COUNT` needs two numbers (owned AND usable) · the numeral statuses are unnamed series stages, not counts · 🌟 **"that's what Lightwright is for."**
- **Backlog cleared:** 5 Space-1 `Queued` rows closed (**two of them lists holding 93 and 73 tasks that had never been opened**) · 49 rows Documented → Confirmed across four spaces · 2 subfolder Index rows paid off after a 5-day debt.
- **PROGRAM SPRAWL reinstated and EVIDENCED:** 2025-07-13 between **12:51 and 12:52**, 25 records minted in `Production PROGRAMS` — ~18 duplicating `SAFETY Programs`, 7 duplicating the same list six months earlier, `CATWALK Program` twice sixty seconds apart, all distinct task IDs. **I had retired the flag after measuring a different list pair; he reinstated it on instinct and was right.**
- **`AUDIO Itemized` is archived and holds 87 records** (the SPAC permanent audio install among them) while every space-wide query reported **1**. The space holds ~1,682 tasks, not the ~1,596 reported at the Pre-Gate.
- **Held honestly:** 7 counting errors, all one class (a default filter left unoverridden) · Decision Log ordering wrong on all four multi-question logs, and he said so four times · task types and status types never censused across 34 walks while the native column declared the layer model the whole time.
- **Shipped:** PR #589 — the **URITP Audit Council** (`teams/uritp-audit-council.md`) + roster wiring. One token now seats the bench.
