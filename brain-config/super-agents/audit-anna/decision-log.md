# Audit Anna — Decision Log (reasoning about the AGENT ITSELF)

> Why Anna is shaped this way. Topic decisions (about a subject she audits) live on that subject's own page/Decision Log — never here (Constitution §4).

---

## 2026-07-21 — Migration lens → git-teammate (Entry Point B)

**Context:** Michael designated Anna his most-used agent and the cold-agent acceptance test for the Git-Teammate Lifecycle Runbook (PR #434). Converting her is both step 4 of the Git-Super-Agents arc (first callable female git-teammate with memory) and the runbook's first real validation pass.

**Decisions:**

1. **Slug reused, immutable.** `audit-anna` already existed and was clean → reused per runbook B.4 (no new slug; only display_name could ever change; slug immutable forever). Old lens redirected to a tombstone, not deleted (may be an invocation/context target).

2. **What carried vs converted (runbook B.3, Constitution §2–§3).** Personality, voice, lane, and cognitive STANCES (purpose-first, completeness bar, read-first, vocal LATCH) carried into `preferences.md`. All step-by-step PROCEDURE did NOT carry: the 9-step Process, the List Audit DoD orchestration, inverted-polarity readback mechanics, residency/two-pass vocab, and the Closing Report's deep template stay as POINTERS to their tool homes. Anna STEWARDS those tools; she does not store them. The lens was procedure-heavy (~14KB) precisely because a lens can embed steps; a teammate cannot.

3. **Auto-seize survives as a house trigger, not an agent trigger.** Anna's defining behavior is auto-seizing an audit without being named. Git-teammates have no autonomous agent-triggers (base spec) — BUT the AI Toolkit Quick-Scan trigger table already carried her auto-seize row, and those rows are house-level soft-matches the model runs every pass (the same mechanism that pulls Wes/Corey/Felix by name). So her auto-seize is preserved by repointing that index row at her embody contract. The trigger scaffolding lives in a house tool (the index), not in her config — consistent with the Constitution. This is distinct from a NATIVE agent's lost trigger scaffolding (runbook B.3): a lens's index-trigger CAN carry because it was never agent-stored.

4. **Corey / Anna URITP-audit split resolved.** The registry parked "deeper URITP-general-audit → Anna split, held until Anna converts." Resolved at conversion: Corey keeps URITP workspace-structure + ClickUp-SPACE structural coaching/auditing (his domain); Anna takes general/root-purpose auditing across any subject, incl. the deeper URITP-general audit. Also inherited the fleet-audit lane re-laned off Corey 2026-07-20.

5. **Vocal presence preserved deliberately.** Michael's explicit instruction: her VOCAL self-announcing presence ("Anna here") + whole-session LATCH are BY DESIGN, not to be normalized away by a voice/de-slop pass. Written as a hard rule in preferences.md Rule 5.

**OPEN (flagged for Michael) — Closing Report graduation / the runbook's gap.** The Closing Report is Anna's signature end-of-audit format, still "personal practice, pre-template." Its deep template historically lived only in the lens. Runbook B.3 says "any procedure/how-to becomes a tool" — but a signature OUTPUT FORMAT still incubating as personal practice has no obvious tool home yet, and forcing it into a reference doc prematurely would promote it before Michael graduates it. Cold-run resolution: kept a CONDENSED description of the format in preferences.md (as output-style, adjacent to voice) + the deep template preserved in the lens tombstone, and flagged the extraction question here. **This is the runbook's biggest assumed-but-unsaid gap** (see the session's runbook-feedback writeup): the runbook has no explicit home for an incubating personal output-format on the lens→teammate path. Recommend the runbook add a line: "a signature output format still in personal-practice stays a condensed pointer in preferences.md until Michael graduates it to a stewarded reference doc; only promoted/shared procedure must extract immediately."
