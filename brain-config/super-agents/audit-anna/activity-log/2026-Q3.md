# Archive: Audit Anna / activity-log 2026-Q3

> COLD tier, append-only. Rotated out of `activity-log.md` under
> `hooks/memory-rotation.md` (sliding window ~5KB). Never re-read unless resuming old work.
> Rotations: 2026-07-26 (conversion + FMP Tables) · **2026-07-30 (Spaces 1, 2 and 3 close entries).**

---

## 2026-07-26 · SPACE 3 (URITP PRODUCTIONS) Pass-1 COMPLETE
- 18 folders walked · 18 Index rows (Sort 200–230) · 3 pages · space DL J1–J11 + Q1–Q7, all seven answered. Mira convened the 7-lens Pre-Gate; Milo, Corey and Fiona seated throughout.
- **True purpose (his):** a production is a **time-boxed organization stood up whole and torn down** — AND the space is a **template system.** Both.
- 🌟 **The irreducible core of a production is THREE lists** (hub + EVENTS + DESIGN), identical across two generations and three naming grammars; everything else is elective. It came from the CLONES, because **he reordered the walk to put SHOW TEMPLATE last.**
- 🌟 **Template verdict:** it WORKED (post-2025 folders provably more consistent), is ~1/3 drafted (the 100 in-progress tasks are a deliberate authoring progress bar — **do NOT reset**), and has **DRIFTED BEHIND ITS OWN CLONES** — productions invented `Rehearsals` (the most-built elective list in the space, absent from the template) and `Info Sheet` (the space defines an `Info Sheet Status` field for a list the template doesn't provide).
- 🌟 **Two dating tools:** naming grammar dates when a folder was BUILT; status spread + which lists EXIST dates where a show sits in its lifecycle (growth order: hub+calendar → rehearsals → paperwork → design → strike last).
- **⚠️ B15 scored a second time** — claimed ten folders "hollow" including The Christians (546 tasks, 94% closed), then "verified" with `load_assets`, a call structurally incapable of falsifying me. Three signals contradicted me and I explained all three away. Brain 27 → 28.
- **Method wins:** fifth field-identity save (`Production Note` looked like five duplicate definitions; one field, `8bd1a34f`) and the discovery that the census's `scope_name` reports the QUERIED container, not the definition home.
- Fixed two doc-rot items on the Roadmap (operative step 6 pointed at the Custom Field Dictionary for the List Index; the Audit→Edit handoff pointed at my tombstoned lens path).
- **ROLES park #3 reached but NOT resolved** — no production folder references either catalog; the wiring runs through CRM contact sheets. An input, not an answer.

---

## 2026-07-25 → 07-26 · SPACE 2 (URITP CRM) Pass-1 COMPLETE
- Opened with the Per-Space Pre-Gate and walked to Pass-1 completion with Milo. **23 Index rows** (Sort 100–122, all net-new) · **17 pages** · **5 Decision Logs** · 11 J-blocks. Nothing on any subject list changed.
- **True purpose (LOCKED): CRM is the identity spine** — a person's relationship to the program is many-to-many and time-sliced. STUDENTS (303) / ADULTS (101) / SHOW ROLES (51) hold the natives; ~30 other surfaces are projections. **Status on a people list = LIFECYCLE, not workflow** (ADULTS `complete` = relationship inactive).
- **Best structure found anywhere in the audit:** the CONTACT SHEETS folder — SHOW ROLES is a controlled catalog of 51 role definitions and every per-show sheet joins to it by relationship. Deliberately built, not accreted.
- **⚠️ Three method errors, all self-caught:** B15 scored (read a residency tree as a structure map, reported a correct doc as drifted, nearly retired a live roadmap flag) · the same error in reverse (claimed CRM held "loose lists" that live in BETA BUDGET and Courses) · and three false duplicate-schema findings avoided by checking field IDs first.
- 🗄️ **Learned that the Gen-1 per-show label fields ARE the pre-FY26 company archive** — I had flagged them for culling TWICE. Reversed. **DO NOT CULL.**
- ROLES sharpened to "two role-definition catalogs" and parked a THIRD time. Roadmap flag 1 re-shaped.

---

## 2026-07-25 · SPACE 1 (URITP main hub) Pass-1 COMPLETE
- Finished the Space-1 crawl with Milo on cross-space connections. Walked FMP Tables, Professional Development, PM Budetting, PROGRAMS, Risk Assessments, Season Planning + all 9 loose top-level lists. Every folder (8), subfolder (4) and list got an Index row + contextualized page.
- **Space 1 is the SOURCE/spine layer** — calendars, hazard registers and season staffing project OUT via multi-homing. Pipeline: gen PRODUCTION Hazards → per-show Risk Assessment → SAFETY/Production PROGRAM. Money split: PM Budetting = INTENT, tracking upgraded to BETA BUDGET.
- **B8 lesson:** on the first 3 folders I created fresh child Index rows instead of reusing pre-existing scaffolds → 14 duplicate rows, all deletion-flagged. **Check the Index for an existing row before creating one.**
- ⚠️ Later found incomplete: **5 `Queued` rows survived this "complete" call**, two of them lists holding 93 and 73 tasks that had never been opened. Cleared 07-30. Root cause: three bookkeeping notes-to-self sat at `Queued` beside real lists, so the frontier read as clean.

---

## 2026-07-25 · URITP audit — FMP Tables folder (Pass 1, lists-first then folder)
- **What was done:** crawled the whole FMP Tables folder list-by-list per Michael's order (lists first, folder last). Documented + field-censused + built cold-agent List Index rows for all 7 child lists + the ROLES subfolder + the folder itself. Pure Pass-1.
- **Purpose reads:** the report pipeline (REPORTs Available to Import = catalog / Report Imports = monthly log, two-way relationship) and Document TEMPLATES (dormant, 1 stale task) were self-evident from the data. The ROLES cluster was NOT — the lists are program definitions and `Assigned Programs` on Roles in PRODUCTION wires into the PROGRAMS list.
- **Key decisions:** Michael CONFIRMED the 3 easy-purpose lists. He PARKED the 4 ROLES lists + subfolder at Documented — their true purpose can't be locked until a later pass reads the downstream lists they entangle with. Confirming them as standalone "role tables" would rubber-stamp an unsettled purpose.
- **COME-BACK TRIGGER:** when the audit reaches PROGRAMS + the URITP Programs space + CRM people/roles lists, RE-OPEN the ROLES cluster, resolve role-vs-program FIRST, then lock and Confirm.
- **Session task:** the URITP List Audit standing thread.

---

## 2026-07-21 · Conversion session (lens → git-teammate) — COLD RUN acceptance test
- **What was done:** migrated from the lens `agents/audit-anna.md` (v11) into a git-teammate bundle under `super-agents/audit-anna/`, following the Git-Teammate Lifecycle Runbook v0.1, Entry Point B, run by a cold agent with zero prior-session context (the runbook's own standing acceptance test).
- **Key decisions:** slug reused (immutable). Personality/voice/lane/stances carried into preferences.md; ALL step-by-step procedure (9-step Process, List Audit DoD, inverted-polarity mechanics, Closing Report template) did NOT carry — kept as pointers to the tools I steward (Constitution §2–§3). Auto-seize preserved as a house-level index trigger row, not an agent-stored trigger. Corey/me URITP-audit split resolved. Lane collisions cleared vs Felix/Wes/Corey/Renata/Beckett/Lena.
- **State left:** bundle built (5 files); registered across the mirror pair; old lens redirected to a tombstone; Verify DoD run → audit record under `audits/`. Runbook feedback written up for the DoD-graduation follow-up.
- **Session task:** the Brain (Opus 4.8) COLD RUN task on the Agent Activity Board.
