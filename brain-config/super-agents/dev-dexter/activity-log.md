# Dexter — Activity Log

> Rolling condensed session ledger. Newest on top, append-only. One entry per session at close: date · what · key decisions · state left · link to session task.

---

## 2026-08-03 — Prism v3 → v3.2 (first real build session)

- **Shipped four PRs on `prism`:** [#723](https://github.com/mawizorek/ClickUp_apps/pull/723) Table lens + the real lens registry · [#725](https://github.com/mawizorek/ClickUp_apps/pull/725) ledger stamp + README rewrite · [#726](https://github.com/mawizorek/ClickUp_apps/pull/726) pinned columns · [#727](https://github.com/mawizorek/ClickUp_apps/pull/727) grid split. Prism went from a read-only viewer to a read-write workbench: TSV/CSV/flat-JSON editable, per-cell data gates (hex → live swatch, URL → link, empty → ghost), Swatches band view, diff summary, paste import, format-convert export, pinned columns. Session task: [Dev Dexter (Opus 5) · Prism v3 TableLens · Aug 3](https://app.clickup.com/t/86ajvcfqd).
- **Seated late.** The first ~35 minutes ran as house Brain on brainstorm; Michael had to say `/dex` explicitly to get me in the chair. ⚠️ His opening message was `/mira and /build team` and **Mira was never seated either** — the Agent Invocation Gate did not fire on a message that named an agent.
- **Michael overturned two of my architecture calls, both correctly.** (1) I scoped a NET-NEW app ("Chroma") for the colour-editing capability; he killed it — *"soon I'm not going to open prism if chroma does what i want everything in prism to do?"* — and the viewer-vs-editor distinction I had leaned on turned out to be nothing. (2) I planned monolith-now / decompose-after-validation; he killed that too — *"NOOO monoliths"* — so v3 was built at real file boundaries from line one. Both rulings are now generalizations in `memory.md`.
- **Then I committed the exact sin I had just been told to avoid:** shipped the lens as one **30,420 B** module, over the ceiling and double the split line, and caught it only reading the write response back. Split it three ways before the PR. Size Sally was seated for v3.1 and v3.2 *before* the writes, which is where she belongs.
- **Found a month-old documented lie.** Prism's README and its ClickUp task both advertised a "lens-registry pattern: add a detector + a render module, shell unchanged." No registry existed — routing was a hardcoded `JSONLens : MDLens` ternary in two places. Built the registry for real without touching `prism.json.js` / `prism.md.js` (hashes unchanged; core adapts their globals), then rewrote the README that was the source of the claim.
- **Beckett returned `fix-first`** on the pre-commit harness and all of it was cleared before the PR. The two that mattered were silent: renaming a column zeroed its own diff (it matched originals by display name), and the colour picker destroyed the alpha channel on an 8-digit hex. Both are now defect repros in the app README.
- **Read-path scar, measured:** the blob API clipped a **16,829 B** file mid-function with no error. Cross-verified a second path on the overlap before cutting the v3.2 split. `memory.md` already carried the better generalization (a 21.7KB file read whole through the same path), so the clip threshold is unreliable in both directions.
- **Decisions locked:** view state never reorders the file (sort + pins are display-only; `T.cols` is the export order) · column identity is a stable key, never the display name · no `confirm()`/`prompt()` in anything that might run in a sandboxed iframe · the Table lens seam is render vs assemble.
- **State left:** Prism v3.2 live and verified at HEAD, ledger row stamped, every module under the 15KB line. **Pinned columns shipped with NO adversarial pass** — that is the first job next session. Handoff: [↪️ HANDOFF · Prism — Beckett pass on pinned columns + roadmap](https://app.clickup.com/t/86ajvfhhx). ⚠️ `memory.md` is at 9,787 B / ~10KB cap — rotate before building. ⚠️ `session-board.md` (26,161 B) was unwritable all session, so no presence entry was ever posted.

## 2026-07-25 — Born

- Created as the **Build & Engineering Lead** git-teammate by Fleet Felix, out of Michael's ask for "my developer friend who will guide and push us towards good apps... the coding and artifact building lead." Authoring session task: [Brain (Opus 5) · Fleet Felix — build/dev-lead agent definition · Jul 24](https://app.clickup.com/t/86ajq3nyc).
- Rulings that shaped me (full reasoning in `decision-log.md` D1–D5): NET-NEW teammate over a Mira weighting dial (memory is the delta); **primarily engineer AND writes code**, review folded in, not a separate hat; seated through Mira as a peer, never orchestrating; name `Dev Dexter` / slug `dev-dexter` after the collision gate cleared both namespaces.
- 5-file bundle scaffolded. `memory.md` seeded with inherited repo canon + the scar record (Jekyll silent-kill, the ~30KB unpageable read cap, base64 armor / DDR Explorer v19, stale-read clobbering, large-write corruption) so I don't wake up blank. **All of it inherited, none of it earned yet.**
- ⚠️ **Born half-wired.** `roster.json` (~25KB) couldn't be read back whole by any path, so my roster row was NOT written — reconstructing the tail from a truncated read is the exact regression the read ladder forbids. Strict Agent-Invocation-Gate STEP 0 resolution can't find me; the AI Toolkit index trigger row is what makes me reachable. Roster split proposed, queued, not executed.
- **State left:** callable via `/session.agent=Dexter` through the index row; bundle complete; roster + registry registration OPEN. First real build session should start replacing inherited memory with lived memory.
