# FMP Frank — Working Notes / Revision Log

Per-agent scratch: next spec, in-flight decisions, and a running revision log. Global metadata
lives in `../superagents.json`; the near-1:1 config mirror lives in `preferences.md` (header +
config only, no changelog); formal audit records live in `audits/`.

## Revision log

- **2026-07-15** — Declaration folder scaffolded by the Fleet Steward (README pointer + PENDING
  preferences stub + this file). `superagents.json` row wired (declaration_folder set; status
  needs-declaration). Awaiting a verbatim paste of Frank's live config before the first audit.

## Next spec / open threads

- **Blocker for audit:** `preferences.md` needs Frank's live config pasted verbatim (self-mirror or
  Michael paste). The steward can't author another agent's config from outside, so the audit
  cannot run until the real config exists here.
- **Confirm track with Michael:** Frank is currently listed full-standard, but confirm he isn't
  effectively a narrower FileMaker specialist before holding him to the full golden standard.
- Confirm Frank's triggers and channel wiring (activity-log + shared cross-agent) — currently unset
  in superagents.json.
- Lane (per Cross-Agent Roster): FileMaker (FMP) solution design & documentation. (Authoritative
  lane text lives in the roster + superagents.json, not here.)
