# FMP Fiona — Working Notes / Revision Log

Per-agent scratch: next spec, in-flight decisions, and a running revision log. Global metadata lives
in `../roster.json`; formal audit records live in `audits/`.

## Revision log

- **2026-07-25** — **RENAMED: FMP Frank → FMP Fiona** (display name only; slug `fmp-frank` immutable).
  Michael's ruling, Fleet Build Queue Decision Log **Q6 → A**, executed by Fleet Felix. Reason: bare
  "Frank" resolved HERE while **Fold-in Frank** (the live anti-sprawl gate, cited by name in six
  places) owned no token at all — so the moment this agent went live, "get Frank on this" in a
  brainstorm would have summoned the FileMaker designer instead of the gate. Routine Ricky / Workshop
  Wes failure class, caught before it fired. Bare "Frank" → `foldin-frank`; "Fiona"/"FMP" → here.
  Also: **the verbatim-native-mirror blocker was RETIRED** (struck, not deleted, in `preferences.md`)
  and the README repointed off `superagents.json`/`index.html`.
- **2026-07-15** — Declaration folder scaffolded by the then-Fleet-Steward (README pointer + PENDING
  preferences stub + this file). Roster row wired; status `needs-declaration`.

## Next spec / open threads

- **NOT a blocker anymore.** ~~Awaiting a verbatim paste of the live ClickUp config.~~ Struck
  2026-07-25: that model was abandoned fleet-wide when Mainstage Milo was built FRESH from the
  Definition Playbook rather than mirrored from an over-hatted native. She is built from scratch.
- **The actual next step: run Entry A (DEFINE) of `gates/git-teammate-lifecycle-runbook.md`.** Pin ONE
  singular lane in a definition brainstorm BEFORE authoring any file. Then Build → Register → Verify.
  Felix stewards; Michael rules on the lane.
- **Lane seam to write at definition time (Michael, Q7):** Fiona = FileMaker **solution design +
  documentation**. **Dev Dexter** = repo-app architecture, data-modelling, code. Documented TANDEM, not
  a hierarchy: FileMaker solution patterns inform repo-app design and back. Mira seats both,
  class-blind. Dexter's profile already carries the mirror of this seam — keep them consistent or the
  seam rots.
- **Confirm at definition:** full-standard agent vs narrower FileMaker specialist; and Michael must
  waive the retired native's trigger scaffolding (git-teammates have no autonomous triggers).
- **Blocked on Michael (manual, irreversible):** disable/delete native ClickUp agent `-39958890` in the
  UI once she lands.
