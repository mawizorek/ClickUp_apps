# FMP Fiona — Declaration Folder (⏳ NOT BUILT)

**Slug:** `fmp-frank` (immutable) · **Display:** FMP Fiona · **Class:** super-agent · **Status:** `needs-declaration` · **Renamed:** 2026-07-25 from "FMP Frank"

> **No metadata lives in this file.** All identity/class/status/lane metadata is the single source in
> [`../roster.json`](../roster.json) under `slug: fmp-frank`. Nothing here is hand-mirrored from there.
> *(Repointed 2026-07-25: this README used to point at `../superagents.json` — renamed to `roster.json`
> on 07-24 — and at `../index.html`; the renderer is now `../roster.html`.)*

**She has never run.** `preferences.md` is a placeholder, not a profile — do not invoke or compose behavior from it.

**Why the folder name and the display name disagree:** slugs are immutable forever, renames touch `display_name` only (Red Rhett lesson). The rename freed the bare **"Frank"** token for `foldin-frank`, the live anti-sprawl gate. Michael's ruling: Fleet Build Queue Decision Log Q6 → A.

Folder contents:

- **`preferences.md`** — placeholder + the rename record + the struck-through native-mirror instruction that was blocking her build.
- **`working-notes.md`** — next spec / open threads / revision log.
- **`audits/<slug>.<date>.md`** — dated audit records (one per audit, via PR). None yet; the audit cannot run until she exists.

**How she actually gets built:** [`../../gates/git-teammate-lifecycle-runbook.md`](../../gates/git-teammate-lifecycle-runbook.md) Entry A (DEFINE — pin ONE singular lane first), then Build → Register → Verify. Built FRESH, not mirrored from the retired native (the Milo precedent). Audit DoD: [`../audit-instruction.md`](../audit-instruction.md) → git-teammate track. Fleet view: [`../roster.html`](../roster.html). Steward: [`../fleet-felix/`](../fleet-felix/).
