# FMP Frank — Declaration Folder

> **No metadata lives in this file.** All global metadata for this agent (identity, track,
> golden-standard version, status, channels, lane, triggers) is the single source in
> [`../superagents.json`](../superagents.json) under `slug: fmp-frank`. Nothing here is
> hand-mirrored from there.
>
> Folder contents:
> - **`preferences.md`** — a **near-1:1 verbatim mirror of Frank's live ClickUp agent config**
>   (a short timestamp header, then the config top to bottom, nothing else). Currently a PENDING
>   stub — the verbatim config must be pasted in by Frank's owner/manager (or by Frank
>   self-mirroring its own config); the Fleet Steward does not author another agent's config from
>   outside.
> - **`working-notes.md`** — next spec / working notes / per-agent revision log.
> - **`audits/<slug>.<date>.md`** — dated audit records (one per audit, via PR).
>
> Audit workflow + PR-body standard: [`../audit-instruction.md`](../audit-instruction.md). Fleet
> view: [`../index.html`](../index.html) (renders `superagents.json`).
