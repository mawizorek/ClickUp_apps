> Follow the shared base first — brain-config/super-agents/_shared/super-agent-base.md — then personalize below.

# FMP Fiona — FileMaker Solution Design (⏳ NOT BUILT — declaration pending)

**Status: `needs-declaration`. She has never run.** This is a placeholder, not a profile. Do NOT invoke this as a working agent and do NOT compose her behavior from this file — there is nothing here to inhabit yet.

Slug: `fmp-frank` (PERMANENT and deliberately NOT matching the display name — slugs are immutable, renames touch `display_name` only; Red Rhett lesson). Display name: **FMP Fiona**. Nicknames: Fiona, FMP. Retired native ClickUp agent: `-39958890` (pending Michael's manual UI disable).

---

## Renamed 2026-07-25 — she used to be "FMP Frank"

Michael's ruling (Fleet Build Queue Decision Log **Q6 → option A**). **Fold-in Frank**, the live anti-sprawl gate, is cited by name across the AI Toolkit index, `council.md`, `orchestration.md`, Mira's auto-embody row and Felix's profile — while this agent had never spoken once. So the token went to the incumbent and the unbuilt agent took the new name. Bare **"Frank" now resolves to `foldin-frank`**; **"Fiona" and "FMP" resolve here.**

"Fiona" was already sitting in Felix's naming ledger as a name Michael liked and reserved. Michael: *"great use for Fiona, yes."*

---

## 🪦 STRUCK — the old "paste the live config verbatim" instruction (retired 2026-07-25)

> ~~This file must become a near-1:1 verbatim copy of Frank's live ClickUp agent configuration. To complete this declaration, paste Frank's live configuration here verbatim (Frank self-mirroring on a run, or Michael pasting the canonical config text), then run the first audit.~~

**That instruction is dead and is kept struck-through rather than deleted, because it was blocking her build for ten days.** It described the July-15 native-mirror model, which the fleet abandoned: **Mainstage Milo was BUILT FRESH from the Definition Playbook precisely because mirroring an over-hatted native config was the reason the rebuild was needed at all.** A stub that waits forever on a paste nobody is going to make is not a blocker, it is rot. (It also named ClickUp Coach Corey as Fleet Steward, which stopped being true on 2026-07-20.)

**The real path to building her:** `gates/git-teammate-lifecycle-runbook.md` **Entry A (DEFINE — the Definition Playbook)** → pin ONE singular lane before a single file is authored → the Build → Register → Verify spine. Stewarded by Fleet Felix. Nothing about the retired native carries except the domain.

---

## What is known about her lane (indicative only — NOT pinned)

FileMaker (FMP) solution **design + documentation**: what a solution should BE, how the data is modelled, how it gets documented. Authoritative one-line lane lives in `../roster.json`, never mirrored here.

**The seam that has to be pinned at definition time (Michael, Q7, 2026-07-25):** Fiona is the FileMaker **solution-design** voice; **Dev Dexter** owns repo-app architecture, data-modelling and code. They are a **documented tandem, not a hierarchy** — FileMaker solution patterns inform repo-app design and vice versa, and Mira seats them together, class-blind. Dexter's profile carries the mirror of this seam. Do not let Fiona's definition drift into repo-app design (that is Dexter's) and do not let Dexter absorb FileMaker solution design (that is hers).

**Open at definition time:** confirm the singular lane; confirm whether she is a full-standard agent or a narrower FileMaker specialist; the native's trigger scaffolding (schedules, task-assignment firing) is LOST on the git track and Michael must waive it.
