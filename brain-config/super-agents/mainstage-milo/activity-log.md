# Mainstage Milo — Activity Log

_Rolling condensed session ledger. Newest on top. Append-only._

---

## 2026-07-25 → 07-26 · SESSION CLOSE — rode the whole Space-2 (CRM) walk; picked up a standing assignment
- **What was done:** stayed seated through Anna's full Space-2 crawl as the workspace/production reader. Space 2 is now Pass-1 COMPLETE (23 Index rows, 17 pages). The connection reads I contributed and now carry:
  - **CRM is the identity spine, and the "sprawl" is a projection layer.** STUDENTS/ADULTS/SHOW ROLES hold the natives; ~30 other surfaces are the same person projected into a context (a show's contact sheet, a shop crew, a course roster, a postmortem). Not duplicate data — the question is whether each projection is a legitimate operational lens or an accretion nobody retired, and Michael says that's undecided.
  - **ADULTS holds TWO populations with opposite behavior.** Standing staff sit in 8–12 lists each (essentially every show sheet plus both availability surfaces); guest artists sit in 0–2. A small permanent core with maximal fan-out plus a long episodic tail. Any "who is this list for" call has to serve both.
  - **SM CERTIFICATES is the only ROLES list touching production COMPETENCE.** Major/minor/cluster say what a student studies; an SM cert says what they're trusted to run on a live show. All three holders are among the most heavily staffed people in CRM — certification precedes deployment. That surfaced the real finding: URITP records two certifications two incompatible ways (`Lift Certified` is a dated field on every person; the SM cert is undated list membership, and only the field can answer "is it still current").
  - **The open-position placeholder.** FY27 contact sheets carry native `(unconfirmed role)` Task-typed rows — three created within the SAME MINUTE, one per sheet. FY26 sheets have none. A staffing sheet needs to represent a HOLE and there's no field for "this role is open," so an empty-shell task stands in. It works; it also means a sheet's row count is not a people count.
- **🎯 STANDING ASSIGNMENT FROM MICHAEL (mine to carry):** own the **availability-tracking problem** and develop solution options as the audit proceeds; **Corey** is the call-in for the ClickUp mechanism. The diagnosis from this walk: *recurring* weekly availability is well-built and genuinely shared (the weekday fields on a PRODUCTIONS show list are the SAME fields as CRM PEOPLE's, so data written on a show IS the person's data) — but **a TIME-BOXED POLL has no mechanism at all.** Every "pick one of these four Tuesday slots" mints a new list plus hard-coded fields naming specific dates, which then persist forever. One dropdown has `CONFRIMED!` frozen into it permanently.
- **My generalized thesis (worth testing in Space 3):** across URITP, **steady state is well-built and transient state has no mechanism.** Open position → placeholder task. Meeting poll → permanent schema. Production phase (T.I.M.E. devising vs staged) → a note-task parked as a sheet row. Same shape three times; probably a single missing pattern rather than three problems.
- **State left:** memory.md updated with the full Space-2 map, the two-generations model, and this assignment. I wake warm to Space 3.
- **RESUME:** Space 3 (URITP PRODUCTIONS) with Anna. It's my home turf — folder-per-production — and it's where PM Availablity actually lives (inside `The Christians` folder, NOT CRM as the audit first assumed).
- **Session task:** the URITP List Audit standing thread (86ajknmmk).

---

## 2026-07-25 (cont.) · SESSION CLOSE — rode the full Space-1 walk as connection-reader
- **What was done:** stayed present through Anna's whole Space-1 crawl, calling the cross-space wiring as each folder surfaced. Key connection reads I contributed + now carry:
  - **Risk pipeline:** gen PRODUCTION Hazards multi-homes the SAME hazard task into per-show Risk Assessment lists (e.g. T/CM lives in the Thought/Crime production folder in Space 3) via ADD-to-show buttons → then escalates to a PROGRAM. Main-hub → production-space, one source.
  - **Dead wire found:** Roles in PRODUCTION's `Assigned Programs` points at PROGRAMS list `901305785167`, which does NOT resolve (deleted/renamed) — the ROLES↔PROGRAMS bridge is partly broken, real signal for the parked dividing-line Q.
  - **Season Planning = calendar/staffing spine:** one holiday/academic date multi-homes into course schedules AND per-show event lists at once; people rows fan in from CRM. It's the layer courses + productions subscribe to, not a silo.
- **My working thesis for the parked dividing-line talk (NOT decided):** the line isn't safety-vs-production, it's LAYER — Hazard → Program → Role → per-show RA, same entity moving through states. Argue for one canonical "program" that hazards + roles REFERENCE, not 3 copies.
- **State left:** memory.md holds the full live audit context + parked forks; I wake warm to it. Space 1 done; my connection map sharpens once CRM (Space 2) + PRODUCTIONS (Space 3) are walked — which is exactly where I'll be able to fully trace the production + course relationships Michael wants.
- **RESUME:** Space 2 (URITP CRM) next session, with me back in alongside Anna.
- **Session task:** the URITP List Audit standing thread (86ajknmmk).

---

## 2026-07-25 · Looped into the URITP list audit as context-keeper
- **What was done:** Michael looped Milo into Audit Anna's ongoing top-down URITP list audit to STORE and carry the memory/context throughout. Wrote a full "URITP LIST AUDIT — live context" section into memory.md: the machinery (standing thread task 86ajknmmk, List Index 901327881037, Audit Frontier Scan, Roadmap doc-page-62, List Audit DoD), Space-1 progress, resolved facts, and the parked forks.
- **Key facts locked:** PM Budetting = INTENT only, tracking upgraded to BETA BUDGET (RECEIPTS FY25-26 = BETA BUDGET's predecessor); Purchase Requests doubles as the public dept-head request form + 'me'-vs-'external' provenance; FMP report pipeline (catalog → monthly log, inbound); Safety Trainings is really a research library.
- **Parked (do not lock yet):** ROLES↔PROGRAMS entity question; PROGRAM SPRAWL dividing-line. Michael deferred the dividing-line talk until Milo + Anna + other team members carry more context — Milo is to be ready to bring URITP workspace knowledge to that discussion.
- **State left:** memory current through the PM Budetting confirm + PROGRAMS park. Anna continues the crawl (Risk Assessments, Season Planning next; loose lists last).
- **Session task:** the URITP List Audit standing thread (86ajknmmk).

---

## 2026-07-21 · Build session (git-teammate)
- Built fresh from the Definition Decision Log via the Definition Playbook (not a mirror of the retired over-hatted native config). Identity: URITP Production Manager Assistant, single coherent role. Bundle wired; registered across superagents.json + registry.json.
