# Mainstage Milo — Activity Log

_Rolling condensed session ledger. Newest on top. Append-only within the window._
_Sliding window ~5KB (`hooks/memory-rotation.md`). Older entries: `activity-log/2026-Q3.md`._

---

## 2026-07-26 · Space 3 (URITP PRODUCTIONS) session — LIVE
- Session agent **Maestro Mira** (orchestrator front door), seated alongside **Audit Anna** (audit lead), **ClickUp Coach Corey**, and **FMP Fiona** — her first seating, and the resolution of the "who is Fiona?" open reference this audit carried three times.
- **My bundle was rotated at the top of the session** by Memory Maggie on Michael's instruction: memory.md 11.3KB → 9.3KB (Space-2 walk detail graduated to `memory/archive/uritp-crm-space2.md`), activity-log rotated to a 2026-Q3 cold archive. Done BEFORE Space 3 loads new context in, not after.
- Session task: the URITP List Audit standing thread (86ajknmmk), reopened not recreated.
- *(entry grows per reply through the session)*

---

## 2026-07-25 → 07-26 · SESSION CLOSE — rode the whole Space-2 (CRM) walk; picked up a standing assignment
- **What was done:** stayed seated through Anna's full Space-2 crawl as the workspace/production reader. Space 2 is now Pass-1 COMPLETE (23 Index rows, 17 pages). Full walk detail now lives in `memory/archive/uritp-crm-space2.md`; the four connection reads that were mine:
  - **CRM is the identity spine and the "sprawl" is a projection layer** — STUDENTS/ADULTS/SHOW ROLES hold natives, ~30 other surfaces are the same person projected into a context. Whether each projection is a legitimate lens or unretired accretion is Michael's undecided question.
  - **ADULTS holds TWO populations with opposite behavior** — standing staff in 8–12 lists each, guest artists in 0–2.
  - **SM CERTIFICATES is the only ROLES list touching production COMPETENCE**, which surfaced the real finding: URITP records two certifications two incompatible ways (`Lift Certified` dated field vs undated list membership).
  - **The open-position placeholder** — FY27 sheets carry native `(unconfirmed role)` Task rows, three created in the same minute. A staffing sheet needs to represent a HOLE and there's no field for it, so an empty shell stands in. A sheet's row count is not a people count.
- **🎯 STANDING ASSIGNMENT FROM MICHAEL (mine to carry):** own the **availability-tracking problem**; Corey is the call-in for the ClickUp mechanism. Recurring availability is well-built and genuinely shared; a **time-boxed poll has no mechanism at all**, so every poll mints permanent schema.
- **My generalized thesis (test in Space 3):** steady state is well-built across URITP, transient state has no mechanism. Open position → placeholder task; meeting poll → permanent schema; production phase → note-task as a roster row. One missing pattern, not three problems.
- **State left:** memory.md carried the full Space-2 map, the two-generations model, and the assignment. Woke warm to Space 3.
- **Session task:** the URITP List Audit standing thread (86ajknmmk).

---

## 2026-07-25 (cont.) · SESSION CLOSE — rode the full Space-1 walk as connection-reader
- **What was done:** stayed present through Anna's whole Space-1 crawl, calling the cross-space wiring as each folder surfaced. Key connection reads I contributed + now carry:
  - **Risk pipeline:** gen PRODUCTION Hazards multi-homes the SAME hazard task into per-show Risk Assessment lists (e.g. T/CM lives in the Thought/Crime production folder in Space 3) via ADD-to-show buttons → then escalates to a PROGRAM. Main-hub → production-space, one source.
  - **Dead wire found:** Roles in PRODUCTION's `Assigned Programs` points at PROGRAMS list `901305785167`, which does NOT resolve (deleted/renamed) — the ROLES↔PROGRAMS bridge is partly broken, real signal for the parked dividing-line Q.
  - **Season Planning = calendar/staffing spine:** one holiday/academic date multi-homes into course schedules AND per-show event lists at once; people rows fan in from CRM. It's the layer courses + productions subscribe to, not a silo.
- **My working thesis for the parked dividing-line talk (NOT decided):** the line isn't safety-vs-production, it's LAYER — Hazard → Program → Role → per-show RA, same entity moving through states. Argue for one canonical "program" that hazards + roles REFERENCE, not 3 copies.
- **State left:** Space 1 done; my connection map sharpens once CRM (Space 2) + PRODUCTIONS (Space 3) are walked.
- **Session task:** the URITP List Audit standing thread (86ajknmmk).
