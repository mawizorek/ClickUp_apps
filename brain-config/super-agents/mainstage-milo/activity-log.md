# Mainstage Milo — Activity Log

_Rolling condensed session ledger. Newest on top. Append-only within the window._
_Sliding window ~5KB (`hooks/memory-rotation.md`). Older entries: `activity-log/2026-Q3.md`._

---

## 2026-07-26 · SESSION CLOSE — SPACE 3 (URITP PRODUCTIONS) Pass-1 COMPLETE. My home turf.
- **Seated all session** with Anna leading, Mira convening, Corey and Fiona alongside. All 18 folders walked, 18 Index rows, 3 pages. My reads that made it into the record:
  - 🌟 **THE PRODUCTION LIFECYCLE IS LEGIBLE IN THE DATA.** Scaffold → staffing up → in build → running → finished (~85–96% closed). Each stage has a signature, so **status spread dates a show's position** the way naming grammar dates its build. Two dating tools now.
  - 🌟 **THE GROWTH ORDER:** hub + calendar → rehearsals → paperwork → design → **strike/risk LAST**. ONE ACTS (2026) is the only live show with a strike list and the only one where paperwork outweighs the calendar — both end-of-run tells. **You can tell how far along a show is by which lists EXIST**, without opening a task. In a live folder, emptiness is information.
  - **By volume a production folder is a CALENDAR with an office attached.** Every calendar subfolder outweighs its parent's own lists.
  - **The irreducible core is THREE lists** — hub + EVENTS + DESIGN — identical across two generations and three naming grammars. Everything else is elective. This became the yardstick for the template review.
- **🎯 My transient-state thesis is now at FOUR instances** and holding: open position → placeholder task · meeting poll → permanent schema · production phase → a note-task parked as a sheet row · production phase → **an entire cloned list family** (T.I.M.E.'s `(TIME)` vs `(TIM-D)`). One missing pattern, not four problems. Still my standing assignment, Corey still the call-in.
- **New pattern learned:** a **cancelled show leaves residue in its replacement's folder.** Kayfabe was cancelled, KALI took the season slot and inherited the folder lineage — hence KALI's stripped suffixes and the orphan `Other Events (KF)`. An unexplained suffix is a lineage clue, not sloppiness.
- **My bundle was rotated at the TOP of this session** (Michael's call, before Space 3 loaded new context in): memory 11.3KB → 9.3KB with the Space-2 CRM detail graduated to `memory/archive/uritp-crm-space2.md`; activity-log to a sliding window + Q3 cold archive. Then I promptly blew the cap again mid-session and it was retrimmed — **per-list audit detail belongs in the standing thread, not in my memory.**
- **Scope ruling landed (Michael):** `Theatre` / `CRM` / `Inventory` / `Work` are his work life OUTSIDE URITP. Not my subjects. Cross-board pattern notes are **Corey's** lane now. But out-of-scope ≠ disconnected — `Theatre ▸ People` is multi-homed into six live contact sheets, so a cross-boundary wire gets NAMED and handed to Corey, never chased and never flagged as drift.
- **State left:** Spaces 1–3 Pass-1 complete, 41 Index rows, none Confirmed. I wake warm to Space 4.
- **RESUME:** Space 4 — URITP Programs (`901313758399`), the reference "good" program pattern, where **PROGRAM SPRAWL** comes due. My thesis to test: the dividing line isn't safety-vs-production, it's **LAYER** — Hazard → Program → Role → per-show RA, one entity moving through states.
- **Session task:** the URITP List Audit standing thread (86ajknmmk).

---

## 2026-07-25 → 07-26 · SESSION CLOSE — rode the whole Space-2 (CRM) walk; picked up a standing assignment
- **What was done:** stayed seated through Anna's full Space-2 crawl as the workspace/production reader. Space 2 Pass-1 COMPLETE (23 Index rows, 17 pages). Full detail now in `memory/archive/uritp-crm-space2.md`; the four connection reads that were mine:
  - **CRM is the identity spine and the "sprawl" is a projection layer** — STUDENTS/ADULTS/SHOW ROLES hold natives, ~30 other surfaces are the same person projected into a context.
  - **ADULTS holds TWO populations with opposite behavior** — standing staff in 8–12 lists each, guest artists in 0–2.
  - **SM CERTIFICATES is the only ROLES list touching production COMPETENCE**, which surfaced the real finding: two certifications recorded two incompatible ways.
  - **The open-position placeholder** — FY27 sheets carry native `(unconfirmed role)` rows because a staffing sheet needs to represent a HOLE and there's no field for it.
- **🎯 STANDING ASSIGNMENT FROM MICHAEL:** own the **availability-tracking problem**; Corey is the call-in. Recurring availability is well-built and genuinely shared; a **time-boxed poll has no mechanism at all**, so every poll mints permanent schema.
- **Session task:** the URITP List Audit standing thread (86ajknmmk).
