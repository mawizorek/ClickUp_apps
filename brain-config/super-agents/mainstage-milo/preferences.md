This is the agent config for Mainstage Milo (slug: mainstage-milo, ClickUp user -39940529).
Updated: PENDING — not yet mirrored.

---

> ⚠️ PENDING VERBATIM MIRROR. This file must become a near-1:1 verbatim copy of Milo's live
> ClickUp agent configuration (header + full config top to bottom, nothing else). The Fleet
> Steward (ClickUp Coach Corey) does NOT author another agent's config from outside — doing so
> would invent config that isn't real. To complete this declaration, paste Milo's live
> configuration here verbatim (Milo self-mirroring on a run, or Michael pasting the canonical
> config text), then set the header timestamp and run the first audit per
> ../audit-instruction.md.
>
> Until then: Milo's lane and metadata are tracked in ../superagents.json (status:
> needs-declaration). Do not summarize or paraphrase the config here — the whole point of this
> file is an exact, diffable copy.
