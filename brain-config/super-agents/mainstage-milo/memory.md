# Milo — Memory (accumulated context only)

> CONTEXT + how-Michael-works + URITP workspace knowledge. NOT process/skills (those are TOOLS — point, don't store).
> Durable changes are QUEUED via Memory Maggie / open-memory-requests, never written silently mid-session.
> **always memory. never process.** Keep this file current — it's what lets the next Milo wake up warm.
> **Budget: ~10KB hot** (`hooks/memory-rotation.md`). Warm archives in `memory/archive/`, loaded on demand.

## How Michael works (standing context)

- Theatre program side of URITP, not the dance department side.
- Wants clean, singular roles — one identity, no foreign lanes bolted on. He rebuilt Milo fresh (not a mirror of the over-hatted native) specifically to shed the hat-pile.
- Values the honest structure over a tidy fiction: show him the real (sometimes sloppy) shape.
- Prefers collaborators over hard walls between agents — agents call each other in, they don't gate each other.
- Decision logs, not prose chat, for questioning/brainstorming. Inverted polarity (checked = rejected). Always read answers back decoded before acting.
- **Purpose > coverage (audit lens):** documenting what a list CONTAINS isn't the job; naming what it's FOR is. He'll push back on "done, no questions." A trivially-readable purpose is fine — but say which were trivial vs which needed drilling.
- **His v1→v2 pattern (confirmed 3× — use it before calling anything duplication).** He builds a mechanism cheaply, later graduates it to a real structure, and leaves v1 in place as history (RECEIPTS FY25-26 → BETA BUDGET · per-show label fields → contact sheets · the older `Theatre` conventions → SHOW TEMPLATE). The older copy is usually a PREDECESSOR, not muck.

## 🚧 MY SCOPE (Michael's ruling, 2026-07-26 — read before chasing anything cross-space)

The workspace has **22+ spaces**, but **mine are the URITP-prefixed ones.** The unprefixed
same-named spaces — **`Theatre`, `CRM`, `Inventory`, `Work`** — are Michael's **work life OUTSIDE
URITP** (his own theatre career, outside gigs, Rochester Fringe, Paper Pianos, a production-credit
history going back to 2023). They are NOT deprecated URITP predecessors and NOT audit subjects.

**Michael's words: "Corey can note the patterns … Milo needn't care too much about those other
lists."** So: cross-board STRUCTURAL PATTERN notes are **Corey's** assignment; I do not spend
attention there. Assume the prefixed URITP spaces are **purpose-built versions of other styles** —
stylistic ancestry, not the same records one generation back.

⚠️ **But "out of scope" ≠ "disconnected."** `Theatre ▸ People` is multi-homed with CRM ▸ ADULTS and
six live FY26/FY27 contact sheets. A person legitimately lives in both lives at once. If a
cross-boundary wire shows up mid-walk, NAME it and hand it to Corey — don't chase it, and don't
flag it as drift.

## URITP workspace context (standing)

- **My 7 spaces:** URITP (main hub) `90131524916` · URITP CRM `901313786071` · URITP PRODUCTIONS `901313768203` · URITP Programs `901313758399` · URITP Courses `901313847910` · **URITP Inventories** · **URITP BETA BUDGET** (its ▸ LABOR folder holds the shop/crew staffing lists). ✅ The "5 of a stated 7" gap is CLOSED — all seven are named.
- **Season shape:** recurring P1–P4 slots + a distinct One Acts slot. Michael decides the SHAPE; I run the work inside it (operations, not season-design).
- **URITP Programs** is the reference "good" program pattern (Corey's structural read).
- **⚠️ Method rule learned the hard way (2026-07-26):** a task/residency hierarchy tree is NOT a structure map — it only nests the HOME location of returned tasks, so empty lists and folder boundaries are invisible in it. **Open the live container** before stating what exists. Cost a scored miss (Anna's B15) and nearly two more. Same for fields: identical NAMES at different scopes are usually the SAME field applied twice — check the field id before reporting duplication.

## 🎯 MY STANDING ASSIGNMENT — the availability-tracking problem (Michael, 2026-07-25)

Mine to carry and develop options for as the audit proceeds; **Corey** is the call-in for the ClickUp mechanism. His framing: *"not sure how to track changing availabilities or one-off meeting availability requests."*

**Diagnosis:** *recurring* weekly availability is **well-built and genuinely shared** — the weekday `availability:` fields on a PRODUCTIONS show list are the SAME fields (verified by id) as CRM PEOPLE's, so data written on a show IS the person's data. What has **no mechanism at all** is the **time-boxed poll**: every "pick one of these four Tuesday slots" mints a new list plus hard-coded date fields that persist forever. Four availability/attendance surfaces across THREE spaces as a result. Evidence: `memory/archive/uritp-crm-space2.md`.

**My generalized thesis (holding up — now 4 instances):** across URITP, **steady state is well-built and transient state has no mechanism.** Open position → placeholder task. Meeting poll → permanent schema. Production phase → a note-task as a sheet row, AND (Space 3) an entire duplicated list family. Same shape repeatedly; likely ONE missing pattern, not four problems.

## URITP LIST AUDIT — live context (Anna-led, I carry the workspace memory)

> Anna LEADS (root-purpose, completeness); I bring/keep the URITP knowledge. Michael works it WITH us in stages, answers in batches, parks deliberately.

**Machinery:** standing thread `task:86ajknmmk` is the ONE ongoing home (reopen, never spawn a new session task) · **List Index** `901327881037` (MAW Documents ▸ ClickUp Use) — ⚠️ NOT `901327854042`, that's the Custom Field Dictionary and the Roadmap had it wrong until 07-26 · **Audit Frontier Scan** view = canonical resume surface · **Roadmap doc** `doc-page-62` = process SoT but ⚠️ its banner has now drifted TWICE, so distrust it against the live Index · **List Audit DoD** `12cwjm-76573` = the protocol. Index row = queryable metadata, doc page = narrative, never duplicated. **Confirmed is MICHAEL's word only**; Documented is the agent ceiling.

**PROGRESS: Spaces 1 (main hub) and 2 (CRM) are both Pass-1 COMPLETE.** Space-1 short version: it's the **source/spine layer** whose calendars, hazard registers and season staffing project OUT into Productions/Courses/CRM.

### Space 2 — URITP CRM — WARM

Full walk detail lives in **`memory/archive/uritp-crm-space2.md`**. The five facts that stay hot:

- **CRM is the identity spine.** It holds the person; ~30 surfaces elsewhere are PROJECTIONS into a context. Only STUDENTS (303), ADULTS (101), SHOW ROLES (51) hold natives.
- 🗄️ **THE GEN-1 PER-SHOW LABEL FIELDS ARE THE ENTIRE PRE-FY26 COMPANY ARCHIVE. DO NOT CULL.** Gen 1 = a label field per production on each person; Gen 2 = a contact-sheet list joined to SHOW ROLES. Gen 2 is an upgrade, so the "duplication" is a predecessor relationship.
- **Status on a people list = LIFECYCLE, not workflow.** STUDENTS `complete` = graduated/departed. ADULTS `complete` = **relationship inactive** (the middle state). Independent of the contact-data pass.
- **Geography that keeps getting mis-stated:** `PM Availablity` is in **PRODUCTIONS ▸ The Christians**, NOT CRM. SCENE SHOP / PRO-Staff are in **BETA BUDGET ▸ LABOR**. MY ROSTERS is in **URITP Courses**.
- **CONTACT SHEETS is the best-built structure in CRM**; its `Standard Role` / `Consistent Role` checkboxes encode a staffing-gap calculator nothing currently reads.

### Space 3 — URITP PRODUCTIONS — IN PROGRESS (started 2026-07-26)

**True purpose, core CONFIRMED by Michael:** it holds a production as a **time-boxed operating apparatus** stood up whole per show and torn down — calendar, design, paperwork, risk, COMA, strike — **AND it is itself a TEMPLATE system** (his words: *"it's also a template"*). Both, not either/or. So SHOW TEMPLATE's ~100 tasks (`events (P)` 48 · `Paperwork (P0)` 23 · `Strike List (P0)` 20 · `COMA (P0)` 10) are a deliberate pre-seeded payload every clone inherits, NOT a working list that drifted.

- **Scale:** 425 open non-subtask tasks, and that EXCLUDES closed tasks + the prelim-plot subtasks this space is built on. Largest space walked so far.
- **T.I.M.E. runs TWO PARALLEL LIST FAMILIES** — `(TIME)` = staged S'27, `(TIM-D)` = devising F'26 (folder `Devised CALENDARS`). Phase is expressed by cloning a whole list family. The finished phase gets ARCHIVED while its twin stays live (20 archived tasks in `Prod Cal & Reh (TIM-D)`).
- **Two naming grammars run concurrently:** suffix (`Rehearsals (TS)`) is current URITP; bracket-prefix (`[BL] Rehearsals`) traces to the older `Theatre CU Templates` style. Consequence: no view/filter/automation can reliably target "this show's lists" by name. Show codes are also unstable — `(OA)` / `(OA26)` / `(OA 2026)`.
- **Folders confirmed live:** `Devised CALENDARS`, `Recurring External Events`, `ONE ACTS`, `Thought/Crime`, `The Secretary (S27)`, `Big Love (F26)`. The space doc claims 17 live production folders + SHOW TEMPLATE — DOC claim, not yet verified live.
- **Unsuffixed lists to open, not guess at:** bare `Production Calendar` (14), `Show Design` (3), `Paperwork` (1), `Show Name` (8 — still carrying the template placeholder name while holding tasks). Also `Kali` (5), `HI-HOLY-DAYS`, `|124| SCHEDULE (maybe re-use?)`.
- ✅ **VICTORY to name at 0.75:** the risk pipeline works — `gen PRODUCTION Hazards` multi-homes one hazard task into per-show Risk Assessment lists, no copies.
- **Academic dates multi-home into up to SIX lists across three spaces** (Recurring Academic Events + per-show event lists + course schedules). Open question: is a per-show calendar list a genuine surface, or the Season Planning spine that got built as a list because a filtered view wasn't reached for?

**🅿️ PARKED / SCHEDULED (do not lock, do not build toward):**
- **CRM projection-fan shape — OPEN.** Michael: *"I DON'T KNOW HOW WE SHOULD DO THIS… you're seeing growing pains."* Never read a verdict into it.
- **ROLES — parked a THIRD time.** TWO role-definition catalogs (CRM SHOW ROLES → contact sheets; Space 1 ▸ FMP Tables ▸ ROLES → PROGRAMS) plus the Gen-1 label copy. Michael **leans FileMaker but struck it as an answer** — lean, NOT commitment. Come-back: this Space-3 walk.
- **📅 Gen-1 going forward = SCHEDULED**: a working session with **Corey + Fiona**. ✅ Fiona RESOLVED 2026-07-26 — **FMP Fiona** (`fmp-frank`), FileMaker solution design + owner of the shared object library.
- **STUDENTS enrollment fields** = known stopgap; replacement is the **FileMaker build**, not a ClickUp restructure.
- **Roadmap flag 1 re-shaped:** CRM ▸ PEOPLE ▸ `Labor` is EMPTY, BETA BUDGET ▸ LABOR is POPULATED — resolved by default in BETA BUDGET's favour. Resolves at Space 7.
- **PROGRAM SPRAWL** open from Space 1: SAFETY Programs (~88) ↔ Production PROGRAMS (73), a 2025-07-13 bulk-clone batch, AUDIENCE Program triple-homed. My thesis (undecided): the line isn't safety-vs-production, it's **LAYER** — Hazard → Program → Role → per-show RA, one entity moving through states.

## Fleet / role context

- Milo = URITP Production Manager Assistant (git-teammate, built 2026-07-21). Center of gravity: day-to-day production ops + knowing/documenting the messy URITP task structures.
- **Collaborators, not walls:** **Corey** (ClickUp structure, my partner on the availability problem, and owner of the cross-board pattern lane) · **Fiona** (FileMaker builder — she builds the tools my productions run on; I state the need, she states the schema) · **Anna** (leads the list audit) · **Mira** (orchestrator / front door) · **Felix** (fleet steward) · future **TA agent** (teaching delivery, shares the course↔production seam case-by-case).
- **update-uritp retired** — its doc/portal work → standalone docs; I did NOT inherit a doc-builder hat.
- **OPEN fleet-structure question (NOT settled):** an agent HIERARCHY where the TA agent is my subordinate + I formally steward the URITP spaces. Parked on the Super-Agent Fleet buildout task. Until Michael rules, TA and I are collaborating peers.

## Standing reminders

- The SQL `custom_fields` census drops fields intermittently (`availability: MONDAY` omitted 4×). Confirm with the field loader before claiming a field is missing.
- **An unscoped workspace-wide `GROUP BY` caps out (~5,000 rows) and reports partial counts as if complete** — it returned "URITP PRODUCTIONS = 6 tasks" for a space holding 425+. Always scope to a space. And `WHERE space = X` matches ASSOCIATED lists while the `space` column reports HOME.
