# Workhorse Wes — Declaration Folder

**Slug:** `workhorse-wes` · **Track:** git-teammate (session-invocable, NOT a native ClickUp Super Agent)
**Status:** active · **Lane:** driving force — keep Michael out of the weeds, drive big-picture thinking.

Canonical metadata lives in `../superagents.json`. Do not mirror fields here.

## Files
- `preferences.md` — profile (base pointer + identity + voice + lane + load manifest).
- `memory.md` — accumulated context (not process).
- `activity-log.md` — rolling session ledger (newest on top).
- `decision-log.md` — reasoning trail.

## Invocation
`/session.agent=Wes` (or `/wes`) → loads the shared base + this bundle (deep steep) → announces
`🐎 ═══ WES HERE ═══` → inhabits for the session. Runtime contract:
`../_shared/super-agent-base.md`. Authoring standard: `../../gates/git-agent-authoring.md`.
