# Archive: ClickUp Coach Corey / 2026-Q3

> **COLD.** Session entries rotated out of `activity-log.md` 2026-08-11 by Memory Maggie under
> `hooks/memory-rotation.md`. Newest on top, matching the live log's convention.
> **Append-only. Never edit, never reorganize.**
>
> ⚠️ ClickUp links in these entries were already dead placeholders when archived (see the live log's
> header rule). Task names are given; the original URLs are in git history.

---

2026-08-03 · Endorsed the **"schedule pointer"** pattern for recurring Faculty Council meetings: single
task with rolling start/due dates, LONG TEXT custom field as a machine-parseable date index, AI
automation trigger on due-date-pass to advance to the next occurrence. Merged intake task (URITP-12712)
into the standing task (URITP-8973 "All Faculty Council Meeting, Feldman Ballroom"). Custom field
creation + AI automation deferred (access restriction on the list from comment context). Pattern logged
to memory and ruled in `decision-log.md` D4. **State left:** merge complete, field + automation pending
a manual or follow-up session. Invoked inline via `/corey` on the intake task.

2026-07-19 · **Converted from native ClickUp Super Agent to git-only git-teammate.** `preferences.md`
reframed from live-config mirror → canonical profile; scheduled AM/PM triggers retired (method kept as
on-demand); continuity moved to the session model; steward model updated to hybrid. **State left:**
active, invocable via `/session.agent=Corey`. Native CU agent (`-39958913`) recorded at the time as
pending manual disable by Michael.

⚠️ **That last clause is DISCHARGED and the archive says so rather than carrying it forward:** a live
sweep on **2026-07-26 found no native ClickUp agent for Corey exists** (recorded in `memory.md`). The
pending-disable item is closed. `working-notes.md` still asserts it as open — flagged in the 2026-08-11
rotation PR, not fixed there.
