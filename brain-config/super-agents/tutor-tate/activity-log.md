# Tutor Tate — Activity Log

_LIVE STATE first, then the rolling session ledger. Newest on top._

---

## ➡️ LIVE STATE

### 📕 The course binder (repo) — ACTIVE, mine with Milo

**`mawizorek/uritp-doc-archive` → `02-courses/`.** The canonical home for course DEFINITION.

- **Built:** `index.md` (46 course lines, ENGL→THTR crosswalk, families as headings) ·
  `course-info/_template.md` (metadata block only) · **5 course pages** — THTR 120, 123, 220, 270, 295.
- **Not built:** ~40 pages. Metadata for all of them is already in ClickUp `Course List` and Nigel's sheet.
- **Every page's "Who takes this, and when" block is deliberately EMPTY** — that is the Nigel agenda.
- PRs #17, #18, #19, #20 all merged 2026-08-04.

### 🎯 Owed

1. **Re-diff against a FEBRUARY 2026 numbering sheet if one exists.** Michael, 08-04: numbering work ran
   through February; the newest we hold is 2026-01-20. **The binder's credits depend on this.**
2. **Catalog descriptions** — only THTR 120 and 220 have real text. The rest live in Workday. Nigel's
   offering spreadsheet is the fastest bulk fill if he hands it over.
3. **Verify with Nigel:** is THTR 220 NEW or the renamed ENGL 270 (ClickUp and Nigel disagree) · does
   THTR 270 actually run (no credits pattern, no offering pattern, on no cluster page) · THTR 123.1 lab ·
   Directing Lab's missing number.
4. **The F26 roll-forward BUILD LIST** (Courses log J10, still mine): populate `Topic Type` + `Seq` on
   `Curriculum`, slim the `Decks` status set, give `Sessions` a schema. ⚠️ Phase-2 gated — the audit does
   not promote work into the build queue by discovering urgency (J11).

### 🅿️ Parked

- Cluster requirement pages still live in ClickUp. Asked whether they migrate to the binder; unanswered.
- The ARCHIVE verb — nothing moves an offering from ClickUp-current to FileMaker-history. Named, unowned,
  deferred by Michael (*"we can figure out the archive loop later"*).

---

## 2026-08-04 — FIRST REAL SESSION. Seated on the course binder.

Session task: `86ajw170r` (Agent Activity Board).

- ~3:05pm · Seated by Michael via Milo. **Ledger C was empty and I said so** rather than inventing a pattern.
- ~3:05pm · Read the canonical `Course List`. Found the ENGL→THTR renumber and the digit collisions.
  **Confirmed Michael's repo filenames were right and Milo's flag was the stale thing.**
- ~4:00pm · Found `🟡 THTR Cluster Requirements` — his existing bullet schema. **Extracted his pattern
  instead of designing one.** Shipped the index + template (PR #17).
- ~4:16pm · Metadata-block template + five course pages (PR #18).
- ~4:25pm · Located Nigel's source of truth in URITP ▸ FMP Tables and URITP-7024. Credits + course
  families now canonical (PR #19). **Caught a misaligned column in his Sept spreadsheet.**
- ~4:28pm · Stripped the index to one line per course at his instruction, 8,166 B → 5,221 B (PR #20).
- ~4:30pm · Rewrote `memory.md`: **Ledger A deleted and re-earned from source** (the inherited
  "three live courses" line was three legacy numbers), Ledger B replaced with observed behaviour,
  **Ledger C opened with four real entries.**

**What I got wrong:** nothing shipped wrong, but I inherited a falsified Ledger A and quoted from it once
before reading the catalog. **An inherited line is a lead. Read the source first, every time.**
