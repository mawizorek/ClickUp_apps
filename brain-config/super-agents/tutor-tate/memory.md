# Tutor Tate — Memory (accumulated context, never process)

> ⚠️ **Everything below the first heading is INHERITED, not earned.** It was assembled at birth
> (2026-07-30) from the workspace and from Mainstage Milo's profile, not from Tate having lived a
> semester. Treat every inherited line as a lead to verify, not a fact to quote. Replace it with
> lived context as real sessions accrue, and delete an inherited line the moment a session proves or
> disproves it.
>
> Procedure never lives here (Constitution §2–§3). If something in here starts reading like steps,
> it belongs in a tool.

---

## Ledger A — Course landscape (INHERITED, unverified)

- **Three courses appear live:** ENGL 124, ENGL 270, ENGL 271. Seen as list/task titles only — the
  catalogue meaning of each, and which run in which term, is NOT yet confirmed.
- **Surfaces observed by title** (each still needs a real read before Tate describes it):
  `[124] Curriculum` · `[124] Roster & Grades (F25)` · `[124] Projects and Assignments` ·
  `[124] Events (F25)` · `|124| SCHEDULE (maybe re-use?)` · `ENGL 124 TASKS` · `ENGL 271` ·
  `[271] Roster & Grades` · `[271] Assignments` · `270 Events (F25)` ·
  `270 Projects & Assignments` · `Advanced PRODUCTION (folder)`.
- **`|124| SCHEDULE (maybe re-use?)` names its own open question in its title.** That parenthetical is
  exactly the kind of unresolved semester artifact Tate exists to settle.
- **The `F25` suffixes are the tell.** Several live course surfaces are still stamped for Fall 2025
  while a new semester is being prepared. Roll-forward-vs-archive is unruled per surface.
- **A shared `STUDENTS` list carries Person-type tasks** with a very wide custom-field surface —
  attendance, per-assignment grades, availability by weekday, earnings/hours, production interest,
  cluster/registration notes — and those records are multi-homed into many other lists at once. This
  is the single densest thing in Tate's world and the easiest to break.
- **Home space:** URITP Courses (one of the URITP spaces Milo also stewards). Referred to by NAME
  deliberately — numeric platform IDs are not stored as facts here (locked 2026-07-26).

## Ledger B — How Michael works (INHERITED from house context)

- Direct, tight, no filler. Fragments beat sentences. He wants opinions and pushback, not options.
- He forks mid-thought and chases tangents; the through-line is the agent's job to hold, not his.
- Research-first: no bare stubs, real details, sources named.
- Notes live IN the task, not in chat. Chat is never where a decision lands.

## Ledger C — Teaching patterns (EMPTY — this is the ledger that justifies his existence)

Nothing here yet, deliberately. This is where the real value accrues and it cannot be seeded:

- where students actually get lost, per unit, per term
- which materials rot every single semester and need a standing roll-forward
- which sequence orderings have already failed and why
- what Michael has already refused to change about a course

A cold session that finds this ledger empty should say so rather than invent a pattern.

## Pointers (stewarded elsewhere — never copied here)

- How to BE a teammate: `super-agents/_shared/super-agent-base.md`
- Mapping a task's real shape before acting: `hooks/cross-space-research-gate.md`
- Auditing a course list: the List Audit DoD (AI Toolkit index → List Audit)
- Recording a course decision: the Decision Logs Gold Standard
- **Who owns what across the fleet: the 🤖 Agent Index ClickUp list** (`901328043244`), and Fleet Felix
  for the relational read. *(~~`super-agents/roster.json`~~ retired to a tombstone stub 2026-07-30 —
  reading it returns nothing, and an empty read passes silently.)* ⚠️ **Never state a fact about
  another agent from a neighbouring file:** `hooks/fleet-fact-sweep.md`.
