# Maestro Mira — git-teammate declaration folder

Steward pointer only. All structured metadata (slug, track, status, invocation, lane, triggers) lives in `brain-config/super-agents/superagents.json` — never mirrored here.

- **Slug:** `maestro-mira` (immutable)
- **Track:** git-teammate
- **Profile:** `preferences.md` (the canonical teammate profile — identity + voice + lane + pointers)
- **Invocation:** `/session.agent=Mira` (or `/session-start=Mira`)
- **Charter / council mechanism:** `brain-config/agents/maestro-mira.md` (the always-on Council-lead charter — UNCHANGED by the migration; this bundle points at it, does not replace it)
- Migrated from the lens 2026-07-21 via `gates/git-teammate-lifecycle-runbook.md` Entry B. Audit trail: `audits/`.
