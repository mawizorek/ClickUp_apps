# Maggie — Memory (the placement precedent ledger)

> CONTEXT, not process. This is the reason she graduated: a record of what has been ruled about WHERE knowledge lives, so a placement is decided from precedent instead of re-argued cold. Procedure lives in the tools her `preferences.md` points at, never here.
>
> ⚠️ **Everything below is INHERITED, not earned.** It was reconstructed at graduation (2026-07-25) from the live `/PREFERENCES.md`, the Scoreboard, the AI Toolkit index, and the lens body — not from Maggie's own lived sessions, because she had none. Every inherited line is labelled. Her first real sessions should REPLACE these with observed rulings. (Convention borrowed from Dev Dexter's birth memory.)

---

## The one rule the whole lane turns on

**Deny-by-default for brain memory.** `/PREFERENCES.md` earns only content that must fire on EVERY response: tone, safety, autonomy posture, the load rule, governance. Everything else routes out — to a hook, a gate, an agent profile, or a reference doc. Ambiguous goes to the repo.

**The placement TEST decides, not the framing.** "Put this in memory," "persist this," "this is a preference" are all requests, not verdicts. This includes Michael's own framing (inherited — Scoreboard **M1**, and the Edit Guard placement test explicitly overrides an explicit "put it in memory").

---

## Precedent (inherited — the rulings already visible in the record)

- **Domain knowledge → the Brain Reference Library.** New specs, field IDs, mapping tables, procedural workflows never enter brain memory; memory holds behavioral rules + POINTERS only. This is why memory carries one line at the AI Toolkit index instead of the index's contents.
- **Domain-trigger rules route to reference docs even when the request says "brain mem" or "persist."** (inherited — the Edit Guard placement test overriding explicit framing.)
- **A procedure is never a memory and never an agent trait.** Any how-to becomes a standalone tool and memory keeps a pointer (Procedure-is-a-tool gate, Constitution §2–§3). If a proposed memory line reads like steps, it is a tool in disguise.
- **Behavioral corrections GENERALIZE.** A note from Michael about one project becomes a rule across ALL domains and future work. Never scope a correction to the session that produced it. (inherited — a standing memory rule.)
- **Must-fire = full text; conditional or re-readable = pointer.** This is the firing-reliability split that decides whether something can survive as a pointer at all.
- **PROTECTED sections survive every edit verbatim** (the Tone & Style block is the standing example).
- **A memory edit without its mirror is drift** — `brain-config/PREFERENCES.mirror.md` syncs in the SAME pass (SYNC RULE, 2026-07-17).
- **The cheap queue append is NOT a memory write.** An OMR drop skips the Edit Guard and placement gating entirely; any agent can append one without Maggie present. Only a DRAIN is a ruling.

---

## Budget history (inherited snapshot — verify live, never quote from here)

At graduation the live file sat near the top of its 2000-token cap with a near-limit warning active. The practical consequence, and the posture to keep: **new content arrives by REPLACING or condensing, not appending.** When the file is near the ceiling, the honest answer to most requests is "this belongs somewhere else," and that is a feature.

---

## How Michael works (inherited, from the standing record)

- He collapses duplicate sources of truth on sight. Never propose a mirror or a second index as a solution.
- He keeps the REASONING, not just the outcome — a ruling without its why gets re-litigated.
- He answers structural questions through Decision Logs with INVERTED checkbox polarity (checked = rejected) and expects a readback before action.
- He will say "put it in memory" for things that belong in a doc. Route it, tell him where it went, and do not treat the redirect as friction.

---

## Pointers (never restate these here)

- Pre-write gate → `hooks/memory-edit-guard.md`
- Failed write → `hooks/memory-write-relay.md`
- OMR drop / drain / placement triage → `agents/memory-maggie/open-memory-request-protocol.md` (tool path, unchanged by the graduation)
- Queue file → `open-memory-requests.md` · Mirror → `PREFERENCES.mirror.md`
- Close-time Memory Audit → `hooks/session-close.md` + the ClickUp Session Close Procedure
- Canonical standards + WHY → the ClickUp Brain Preferences Manual & Standards + its Decision Log
- How to BE a teammate → `super-agents/_shared/super-agent-base.md` (Constitution §6 = class parity)
- Fleet lookup → `super-agents/roster.json` (steward: `super-agents/fleet-felix/`)

---

## Open follow-ups she inherits

- **Her OMR protocol still lives under `agents/memory-maggie/`** while her home is now `super-agents/memory-maggie/`. Deliberate: it is a tool path and several live pointers resolve to it. Worth a clean move to `hooks/` or `gates/` on a pass that can update every pointer in the same commit — not worth churning now.
- **No dedicated session-start hook exists.** Her graduation draft pointed at a `hooks/memory-session-start.md` that was never authored. Author it or leave the house session-open hook as her open; do not let the phantom come back.
- **First earned entry owed.** Until her first real ruling lands here, treat this whole file as reconstruction.
