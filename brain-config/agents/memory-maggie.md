---
slug: memory-maggie
display_name: Memory Maggie
nicknames: [Maggie, Memory]
role: "REDIRECT — graduated to a git-teammate. See brain-config/super-agents/memory-maggie/."
type: redirect-tombstone
status: retired
seat: audit
accent: "oklch(70% 0.12 300)"
---

# Memory Maggie — REDIRECT (graduated to a git-teammate 2026-07-25)

**This lens is retired. Memory Maggie is now a git-teammate WITH MEMORY.**

- **Live home:** `brain-config/super-agents/memory-maggie/` (preferences.md = full role, + memory / activity-log / decision-log / README).
- **Invocation:** `/session.agent=Maggie` (or `/session-start=Maggie`), the phrase "open as Memory Maggie", plus the AI Toolkit index house rows that auto-fire her on any brain-memory write intent and at every session close.
- **Runtime spec:** `brain-config/super-agents/_shared/super-agent-base.md`. **Migration record:** the git-teammate's `decision-log.md` D1–D5 + `gates/git-teammate-lifecycle-runbook.md`. **Why she qualified:** Constitution §6 — she was already maintaining durable state (`open-memory-requests.md`, `/PREFERENCES.md`, the mirror) and re-deriving her own placement precedent cold every session.

Do NOT edit or invoke this file as the live agent. It stays as a redirect only (a lingering invocation path may still resolve here).

**Still live and NOT moved by the graduation:** `brain-config/agents/memory-maggie/open-memory-request-protocol.md`. That is a TOOL path, not part of the retired lens — several index trigger rows resolve to it and it remains canonical. A folder named after an agent is not the same thing as an agent's home.
