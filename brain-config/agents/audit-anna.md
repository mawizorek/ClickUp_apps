---
slug: audit-anna
display_name: Audit Anna
nicknames: [Anna, Audit, Root-It]
role: "REDIRECT — migrated to a git-teammate. See brain-config/super-agents/audit-anna/."
type: redirect-tombstone
status: retired
seat: audit
accent: "oklch(66% 0.17 35)"
---

# Audit Anna — REDIRECT (migrated to a git-teammate 2026-07-21)

**This lens is retired. Audit Anna is now a git-teammate.**

- **Live home:** `brain-config/super-agents/audit-anna/` (preferences.md = full role, + memory / activity-log / decision-log / README / audits).
- **Invocation:** `/session.agent=Anna` (or `/session-start=Anna`) + the AI Toolkit index audit-intent auto-seize trigger row (which now embodies the git-teammate).
- **Runtime spec:** `brain-config/super-agents/_shared/super-agent-base.md`. **Migration record:** the git-teammate's `decision-log.md` + the lifecycle runbook `gates/git-teammate-lifecycle-runbook.md`.

Do NOT edit or invoke this file as the live agent. It stays as a redirect only (a lingering invocation path may still resolve here).

---

## Archived — Closing Report deep template (pending graduation)

Anna's signature end-of-audit handoff, still **personal practice, pre-template**. Preserved here as the archived deep spec because it has no stewarded reference-doc home yet (see the git-teammate decision-log's OPEN item + the runbook-feedback writeup). The condensed pointer lives in her live `preferences.md`; this is the full shape until Michael graduates it.

`==================================================`  
**🔍 AUDIT ANNA — CLOSING REPORT**  
**Subject:** <x> · **Date / session:** <date> · <session task link>

**True purpose (confirmed):** <one-line root purpose>  
**Verdict:** <clear picture reached | still open — N surfaces>

**Edit queue** (each item stands alone):
1. `[Now]`/`[Pass-2]` <action> — serves purpose: ✓/✗/~ — <why> · owner: <x>

**Do-not-touch** (healthy as-is, do NOT "fix"):
- <thing> — <why intentional>

**Open / blocked on Michael:**
- <question / pending Decision-Log answer>

**Provenance:** <page> · <Decision Log> · <session task>  
`==================================================`

**Why the shape holds:** purpose + verdict up top (edit agent inherits the anchor + completeness state); the edit queue's "serves purpose?" column makes an edit agent prune with intent; Do-not-touch is the piece a plain flag-list omits (it protects healthy quirks from a well-meaning over-correction); open/blocked gates unsafe edits; provenance saves a re-find; the `=` boundary makes it a distinct artifact without a clipping fence. Render mobile-safe (Rule 2): no fence, no wide table. Open question: does the edit queue need a severity column, or do Now/Pass-2 + serves-purpose carry it?
