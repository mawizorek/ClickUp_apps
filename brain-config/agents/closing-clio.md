---
slug: closing-clio
display_name: Closing Clio
nicknames: [Clio, Close, Recap]
role: "REDIRECT — graduated to a git-teammate. See brain-config/super-agents/closing-clio/."
type: redirect-tombstone
status: retired
seat: close
accent: "oklch(70% 0.12 20)"
---

# Closing Clio — REDIRECT (graduated to a git-teammate 2026-07-25)

**This lens is retired. Closing Clio is now a git-teammate WITH MEMORY.**

- **Live home:** `brain-config/super-agents/closing-clio/` (preferences.md = full role, + memory / activity-log / decision-log / README).
- **Invocation:** `/session.agent=Clio` (or `/session-start=Clio`), plus the house session-close trigger that seats her at every close. A **bare** `Clio` fires her READ-ONLY mid-session health check; the write-heavy full close needs the close trigger or an explicit instruction.
- **Runtime spec:** `brain-config/super-agents/_shared/super-agent-base.md`. **Migration record:** the git-teammate's `decision-log.md` D1–D6 + `gates/git-teammate-lifecycle-runbook.md`. **Why she qualified:** Constitution §6 — she already maintained durable state (`usage-log.json`, `agents/closing-clio/reports/`) and re-derived her own session-health history cold at every close. Sixth graduation (Wes → Anna → Mira → Maggie → Sage → Clio).

Do NOT edit or invoke this file as the live agent. It stays as a redirect only (a lingering invocation path may still resolve here).

**Still live and NOT moved by the graduation:** `brain-config/agents/closing-clio/reports/`. That is a TOOL path, not part of the retired lens — a folder named after an agent is not the same thing as an agent's home.

**Her procedure never lived here anyway:** the close sequence is `brain-config/hooks/session-close.md`, which she stewards. This file's old audit checklist and output format were personal practice, not the contract; the contract is the hook.
