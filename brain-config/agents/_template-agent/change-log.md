# <Display Name> - Change Log

Version history for the `<slug>` agent. The profile (`profile.md`) stays lean; all version history lives here. Git holds the detailed diffs behind each line.

- <YYYY-MM-DD> - created.
