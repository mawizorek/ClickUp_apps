name = "cast-grid-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# The CLICKUP_TOKEN is stored as an encrypted secret, NOT here.
# Set it with: wrangler secret put CLICKUP_TOKEN
# Then paste your pk_... token when prompted.

[vars]
# Non-secret environment variables (if needed later)
# APP_VERSION = "0.1"
