# &lt;App&gt; — Object Index (rendering manifest)

_Navigable map of every documented object. Mirrors the FileMaker solution._

## Tables (`tables/`)

| Table | Role | Status |
|---|---|---|
| _add rows_ | | |

## Relationships (`relationships/`)
## Scripts (`scripts/`)
## Functions (`functions/`)
## Layouts (`layouts/`)
## Value Lists (`value-lists/`)
## Meta / narrative (`meta/`)
