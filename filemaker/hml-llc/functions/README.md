# Custom Functions

One file per custom function, mirroring *File → Manage → Custom Functions*. Manifest: `_index.json`.

## Format

Signature → Purpose → Parameters → Returns → Body → Related → Changelog.

- [MSG_ValueListErrors](./MSG_ValueListErrors.md)

JSON param/reader helpers (`Param` / `PText` / `PNum` / `PBool` / `PJSON` / `ParamGet…`) are pending migration from the FileMaker JSON Parameter Helper Functions doc.
