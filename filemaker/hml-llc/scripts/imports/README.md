# scripts / imports

Mirror of the FMP **imports** script folder. Orchestrator + preflight import scripts (ALLCAPS orchestrators, `__` namespace). Pending enumeration from the FileMaker file.

> Import-chain awareness: never edit one import script in isolation; understand its place in the sequence first.
