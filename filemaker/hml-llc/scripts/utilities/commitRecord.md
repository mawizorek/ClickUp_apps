# commitRecord — moved

Superseded by the **standard v1.4** script model. The body + all narrative now live in **[commitRecord.fmscript](./commitRecord.fmscript)** (a dictation reference — the exact steps to type into *Manage Scripts* — with the role / when-to-use / design notes / candidate upgrades / changelog carried as native `#` comment lines). The machine row (name / folder / calls / scriptRef) is in **[../_index.json](../_index.json)**.

This breadcrumb exists only so old links don't 404. Do not re-add prose here — one script, one home (`DECISIONS.md` D-005).
