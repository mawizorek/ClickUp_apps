# scripts / utilities

Mirror of the FMP **utilities** script folder. Reusable narrow helpers.

- [commitRecord](./commitRecord.md) — generic commit + validation-error handler (shared cross-app standard helper).
