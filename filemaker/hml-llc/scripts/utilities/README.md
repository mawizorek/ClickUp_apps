# scripts / utilities

Mirror of the FMP **utilities** script folder. Reusable narrow helpers.

- [commitRecord](./commitRecord.fmscript) — generic commit + validation-error handler (shared cross-app standard helper). Dictation reference; machine row in [../_index.json](../_index.json).
