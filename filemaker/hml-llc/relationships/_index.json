{
  "_meta": {
    "app": "hml-llc",
    "folder": "relationships",
    "schemaVersion": "2.0",
    "generated": "2026-07-16",
    "source": "../schema/relationships.json",
    "model": "loan-first",
    "note": "Single edge surface is ../schema/relationships.json (DECISIONS D-006). This manifest holds render hints ONLY and does NOT restate edges — the renderer reads schema/relationships.json directly, the same way the schema viewer reads schema/tables.json. Removing the duplicated edge list kills the triplicate-index drift D-004 killed for calcs."
  },
  "render": {
    "view": "to-graph",
    "nodes": "tables",
    "edge": "child.field -> parent.PrimaryKey",
    "labelBy": "cardinality",
    "colorBy": "status",
    "statusColors": { "locked": "good", "pending": "warn", "under-review": "info" }
  }
}
