# Scripts and Automations

> **PENDING:** script inventory lives inside the FileMaker file. Share DDR output or script steps to populate. Phase 3 will move individual scripts to their own source files under a shared `scripts/` library.

Known script-adjacent items on `GLOBAL_USE_VARIABLES`: `script_Stats_RecordCounts`, `script_LastSaved`.
