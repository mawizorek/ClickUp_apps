/* QR Forge · source/build.js
   The only place the version and PR number are written. The footer stamp reads
   from here, so a stale cached bundle shows a stale stamp instead of lying. */
window.QRF_BUILD = { version: '1.0', pr: 0 };
