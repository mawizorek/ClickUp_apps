# App Ledger — THE single source of truth for every app

**One file. One row per app. Every folder in the repo root appears here or in the not-apps line.** `brain-config/app-index.md` is retired to a stub; there is no second index.

**Also the tiebreaker against stale reads and CDN lag.** When your read disagrees with this file, **re-read at HEAD and reconcile** — the gate cuts both ways (below).

## 📏 Size (target **~16KB**, ceiling **22KB** — LOCKED 2026-07-26, Michael)

**Each row is CURRENT STATE, not version history.** Per-version narrative lives in git history + the PR description + that app's own `README.md`/`next-build-spec.md`. This ledger cites the PR and stops.

- **Ceiling ~22KB** is physics: base64 inflation past the ~30KB read cap. Past it the file cannot be read whole, and **a file that cannot be read whole cannot be safely edited.**
- **Target ~16KB** = floor + real headroom. It replaces a ~12KB target that had **never once been met** since being locked — see the Floor Rule in `brain-config/hooks/source-size-budget-enforcer.md` (a stated target never met is rot, not aspiration).
- **Never split the table** — trim prose. Seat Size Sally before this grows.
- ⚠️ **Every edit that adds must also trim.** The changelog is where to cut: entries older than a week keep their durable finding and lose their narrative, because git log holds the story.

## Procedure (MANDATORY on every app PR)

1. **Before touching, editing, PR-ing, or even DISCUSSING an app:** read this file. Compare against the version you *think* is current.
2. **Mismatch = STOP.** Re-read at an immutable commit-SHA path or the git blob API (base64-decoded). Reconcile before building.
3. **Immediately after opening a PR:** update that app's row in the same session. The ledger must never lag `main`.
4. Advisory-blocking on discussion, hard-blocking on edits.

⚠️ **The gate cuts BOTH ways.** This file is a *claim* about reality, not reality. On 2026-07-25 the `app-dashboard` row still carried a "main regressed, restore pending" warning dated **2026-07-07**; the fix had shipped the next day and the note was **32 PRs stale**. An agent nearly ran a destructive revert off it. **HEAD wins; this file gets corrected.** A warning rots exactly like a version number — verify before acting on a ⚠️, especially a stale-dated one.

⚠️ **An OPEN-ITEMS list rots the same way, and is more dangerous** (2026-07-28). The `f1-racetracks` row carried *"`fastLap` is r9 ONLY — backfill r1-8"* for five days after that backfill completed. **A completed job left in an open-items list is an instruction to redo it.** Verify an open item before working it, exactly as you would a warning.

⚠️ **A row can also be wrong about WHAT an app is** (2026-07-30). The `inciardi-collection` row said *"probably a data/spec folder, not a Pages app"* for four days while it was a live, deployed app being used from a phone. **Never write a speculative classification as a directive.**

⚠️ **AND A ROW CAN BE WRONG ABOUT AN APP'S SHAPE** (2026-08-02). This file said *"THE WORKER IS TWO FILES"* for `inciardi-collection` while it was **six**, and used that claim to instruct *"split before adding any route"* — a remediation aimed at a split that had already happened twice more. **A structural claim rots exactly like a version number, and it is worse, because it reads as architecture rather than as state.**

🔴 **AND A ROW GOES STALE BY DEFAULT, NOT BY ACCIDENT — FOUR MEASUREMENTS IN 72 HOURS.** 07-31: `inciardi-collection` said **v3** while `main` was on **v16**, was corrected to v17, and then **v18 and v19 shipped without touching it again.** 08-01: `f1-racetracks` was found **eight versions and ten PRs behind**. 08-02: `inciardi-collection` was **four versions behind AGAIN** (v19 vs v23) — *the same row, corrected the previous day, stale again within 24 hours.*

> ⭐ **The diagnosis is not laziness, and "try harder" will not fix it. Step 3 fires at the END of work, when the thing you came to do is already done — so it is the step with the least momentum behind it. Step 1 is the enforcement mechanism: if you READ the ledger before touching an app, the gap is unmissable and you fix it on the way in.** All four gaps above were found by step 1 and none by step 3. **A row that is many versions behind is worse than a missing row: a missing row admits it knows nothing.**

**Data-separated apps carry TWO version facts:** the *shell* build (bumped on rebuilds) and the *data* date (bumped on refreshes). A data refresh does NOT bump the shell.

**Version-stamp convention:** carry a version the app itself declares (`APP_VERSION`, or the `?v=` cache-bust on its source links) — **not a PR number, not the word "live."** ⚠️ **When an app declares its version in more than one place, this row must say WHICH ONE it is quoting and name the others as disagreeing.** `f1-racetracks` has five claimants; its row quotes the shell. ⚠️ **And never write a version you have not READ BACK from the app at HEAD** — an estimated version is the same defect class as an estimated byte count.

**⚠️ Read-path note (verified 2026-07-25):** branch raw URLs served copies MANY versions stale AND they flatten HTML/SVG out of template literals. Reliable: blob API base64-decoded, or `get_file_contents` at an immutable commit SHA. **Never build an edit on a branch raw read.**

---

## Ledger

Live URL pattern: `https://mawizorek.github.io/ClickUp_apps/<slug>/`. Unmarked rows are live.

| App (slug) | Version | State + live warnings (detail → PR + app README) |
| --- | --- | --- |
| `agentglass` | shell **v2** · seed 2026-07-25 · **SNAPSHOT** | PR #488. Agent Activity Board liveness feed. Hash-router shell + `pages/` + `source/`; non-executing `server/` tree. **⚠️ NOT PASSIVE YET** — reads a repo-committed seed; header pill reads `live` vs `snapshot` so it can't imply false currency. To go live: deploy `server/worker.ts`, set `LIVE_URL`, bump `?v=`. Capture is 3 labeled tiers (`tagged`/`derived`/`ambient`); derived rows are NEVER billable. **Do NOT: fence-match ClickUp comments (brace-match only); add latency percentiles (order locked by J3).** Telemetry is self-attested — not an audit trail. |
| `app-dashboard` | **v4** (`?v=73`) | PR #72/#74. Slim launcher; thin `index.html` loader over `source/*`. Live tree via GitHub API, health dots, 5-min cache, ClickUp/FileMaker toggle, Show-retired toggle, detail sheet. **Bump `?v=` on every source change.** `supersededBy` in `data.js` must name another APP slug. ⚠️ **Do NOT revert PR #59/#57** — that instruction was stale by 32 PRs; reverting destroys current work (verified at HEAD 2026-07-25, PR #485). |
| `budget-code-mapper` | unverified (last known v6, design) | Verify + stamp a real version on next touch. |
| `f1-racetracks` | shell **v19.1** (circuits `?v=20260801e`, standings + weekend `?v=21`, `app-shell.js` `?v=11`) · **23-round calendar 2026-08-01** · results store verified `d27ce55` (r1-9) | PRs #654 → **#690**. ⚠️ **This row said v11 until 08-01 — eight versions and ten PRs behind.** **🔴 THE DATA LAYER IS FOUR VECTORS — read `f1-racetracks/README.md` ▸ Data Model AND Decision Log J10-J21 before touching any JSON.** CIRCUITS (`circuits/<slug>.json`, timeless) · **WEEKENDS (`season/2026/index_weekends.json` — THIS is the season calendar)** · **SEATS+TEAMS (`season/2026/index_drivers.json`, driver × team × season)** · CLASSIFICATIONS (`f1-results/2026/`, only rounds actually raced). **NOTHING TIME-SHAPED IS STORED:** round, status, display date and the current/last round pointers are derived at boot by `source/08_season.js`, so a cancellation or insertion self-heals. **🚨 NEVER PERSIST A DERIVED ORDINAL — join by `slug` or `cuTaskId`, never by round number** (Bahrain + Saudi cancelled 03-14, F1 renumbered densely, **every entry from Miami on was off by two for eight months**). **Sepang** hosts the *Bahrain* GP, 2-4 Oct; its circuit file is deliberately MINIMAL + sourced-only + `report:false`. **Team colour is read from `shared/themes/colors.tsv` at boot and injected as `--t-<team>` (v16)** — a LOCAL IDENTITY LAYER, never a theme vector, because a theme applies one at a time and the matrix renders eleven teams at once. 🔴 **FIVE VERSION CLAIMANTS, ALL DISAGREEING:** `source/09` says `v12` · `source/standings/data.js` says `v6.0` · `circuits.html` header v19.1 · `README.md` v11 · this row. **The fix is the v20 port** (one `config.json`, footer written from it), NOT five hand-edits — `source/09` is **29,446 B** and `README.md` **26,076 B**, both past the 22KB safe-edit ceiling, and the port rewrites both. ⚠️ Open: **`grid`+`qualifying` absent on r03/r04/r07** (66 driver-rows, SOURCED or absent — do not improvise), sprint arc documented-never-built, sprint `sess` labels disagree with the authoritative boolean on 5 circuits, no `sepang` in `LIVE_CIRCUIT_ALIASES`, **FOUR source modules over the 15KB line**, `chrome-tokens.css` §8 is an `!important` override layer whose exit condition is the port, **`live-tracker.html` is standalone BY DESIGN** (Michael 08-01: *"big enough to be standalone cos it's never worked yet"*) — no spine, will drift, **not a defect**. ✅ **`applyTheme('f1-racetracks')` NOW RESOLVES** (PR #696 — `paddock` colour entity + the join). **The app does not call it yet; that is the port.** 🅿️ **v20 PORT IS PARKED ON `f1-racetracks-v20-port`** — 3 commits, no PR, `config.json` + `chrome.js` placed UNFORKED from `template-app`. **Do not re-cut that branch.** ⚠️ **RICKY:** `cuTaskId` lives on the weekend row and `current_round_slug` is gone; adding a round is write the round file + one `{slug,file}` row. |
| `file-chunker` | unverified (last known v16.2) | Generates the `/source` chunk sets for over-cap apps. Verify on next touch. |
| `inbox-digest-report` | v6 (shell) · data Jul 17 2026 | PR #323. Gmail Inbox Sweep audit surface. **PURE RENDERER of `data/inbox-state.json`** — the sweep edits ONLY the JSON. Field spec in `data/README.md`. |
| `inciardi-collection` | **v23** (`?v=23`, read back from `index.html` at HEAD) · workers deployed · theme `soft-mercedes` · D1 live | PRs #590→**#692**. ⚠️ **THIS ROW HAS NOW GONE STALE TWICE IN TWO DAYS** — v3-vs-v16 on 07-31, then v19-vs-v23 on 08-02, both caught on the way IN. **READ THIS FILE BEFORE TOUCHING THE APP.** Digital twin of the mini-print binder: binder → sheets → sides → 9 slots. **Its OWN worker + D1, separate from `inciardi-market`.** Thin `index.html` router + JS modules + `pages/` partials. **🔴 THE WORKER IS SIX FILES** (`worker.js` gate+dispatch · `reads.js` the GET routes · `images.js` upload/serve/carousel · `links.js` attach/bring-to-front/archive · `adopt.js` the legacy 177 import · `sheets.js` binder mechanics). ~~two files~~ — that claim was 4 versions stale and told you to split something already split twice. **Every one of the six is measured in `index.html`'s module map; re-measure, never estimate.** **🔴 v22 — ONE ORDERING, ONE OWNER:** `photoOrder()` lives once in `links.js` and BOTH `reads.js` (card, `LIMIT 1`) and `images.js` (carousel, no limit) import it, so the card photo IS the carousel's first frame structurally. `is_primary` is **retired from the ranking** in favour of `edition_image.sort` — starring writes `MIN(sort)-1` and you undo it by starring something else. ⚠️ **The column still exists and is meaningless; it goes with migration 002.** **🔴 v23 — the print page has its photographs:** `photoview.js` is a module with **no page of its own** so `photos.js` can call the same component. ⚠️ **`photos.js` DOES NOT CALL IT YET** — a named, deliberate seam, not an oversight. 🔴 **PINCH-ZOOM IS DEFENDED THREE WAYS** in `photoview.js` (swipe abandons on a 2nd touch point · `touch-action: pan-y pinch-zoom` · `gesturestart` never touched). **Never add `maximum-scale` or `user-scalable=no`** — it would break a shipped feature. **ROUTES:** binder · summary · shoebox · enter · photos · **`#artwork?id=`** (detail, in no menu) · `#backroom` (unlisted). **BACK ROOM** bulk-imports from `batches/<slug>.json` — **data, never code**; its 3×3 arrangement is editable, and **the editor writes the RUN, not the FILE** — commit corrected grids back. **🔴 THE WRITE KEY SHIPS PUBLICLY, ON PURPOSE** — `core.js` → `DEFAULT_KEY` **and** `wrangler.toml` → `[vars] WRITE_KEY`, Michael's call. **The pair must match; rotate both or neither.** A mismatch is a **401 on first save, never on `/health`**. ⚠️ **Bare worker URLs have served 24-hour-stale reads**; add `?nocache=`. ⚠️ `README.md` still claims the key is never bundled. ⚠️ **Several front-end files sit over the 15KB split line and `photoview.js` shipped over it on day one** — the ⚠️ rows and their named seams are in `index.html`'s module map. **Do NOT restore a read-only banner in `checkConfig()`, an `ic_color` read, or the back room's number strip.** |
| `inciardi-market` | front-end **v16** · both workers v1.4 · 🪦 **BEING RETIRED** | PR #474. Two Cloudflare workers over one D1+R2 store; **NOT data-separated**. **Michael, 07-31: "consider that app dead."** 🔴 **DO NOT DELETE ITS D1 YET.** `print_image` holds the only R2-key → which-print mapping for the 177 images in `inciardi-images` — **export that join before any teardown** or 268MB becomes anonymous objects. **🔴 OPEN SECURITY: `LOGINS` holds guessable write keys in the public bundle** — distinct from the *deliberate* baked key next door: readable is a tradeoff, **guessable is a defect**. **Image Rendering Law:** `proxied(url,360)` is a no-op on an R2 `/img?key=` URL. Prices are DOLLARS not cents. |
| `markdown-viewer` | **RETIRED** | Superseded by `prism`. Hidden behind Show-retired. **Do not develop.** |
| `on-track` | shell **v2.4** (`?v=24`) · data 2026-07-23c | PRs #463/#468/#470. Data-separated. 2nd theme-spine consumer: composed 4-vector theme, **passive Theme picker** reading `shared/themes/_themes.json` live. **The reference implementation for a theme picker.** `:root` = labeled first-paint FALLBACK FLOOR. **The 20 series colors are a LOCAL data layer.** ⚠️ **08-01: its `on-track` join points at the `red-bull` COLOR row, whose accent moved red → yellow in PR #696** (four teams were sharing one red). **Its chrome accent changed and nobody from this app was in the room.** If it wants the old red it is `accent-2` on the same row, or it takes its own colour entity — **not a local override.** 🐞 **Open bug:** favicon/apple-touch/og:image point at `IMG_*.png` files that are GONE. One-line fix. |
| `pdf-splitter` | unverified (last known v1) | Verify + stamp on next touch. |
| `polish-demo` | demo | Working example of the pro-polish standard. Gate code `2026`. Not a launchable app. |
| `prism` | v1 | PR #54. Data App Viewer (JSON + Markdown). Modular. Gate code `2026`. |
| `report-normalizer` | ⚠️ **unverified, never indexed** | Found 2026-07-25, absent from BOTH former indexes. Needs a version stamp + a real row on next touch. |
| `retrocast` | ⚠️ **unverified, never indexed** | Found 2026-07-25 — despite being the **FIRST theme-spine consumer** per this ledger's own rollout note. Needs a version stamp. |
| `routines` | 🗑️ **DELETED 2026-07-27 — NO LONGER AN APP** | PR #562. The folder survives as **docs only** (schedule, runbooks, `last-run/` stamps) and is listed here purely so the app is not re-added. **Do not rebuild it.** **Its lesson lives in `routines/next-build-spec.md`: the app never rotted — retiring the scheduler is what turned it into a duplicate. When a capability is retired, ask what existed only to compensate for it.** |
| `squash` | ⚠️ **unverified, never indexed** | Found 2026-07-25. Needs a version stamp + a real row. |
| `Vectorworks` | ⚠️ **unverified, never indexed** | Found 2026-07-25. Also the ONLY capitalized folder in a kebab-case repo — needs a naming decision alongside its stamp. |
| `world-cup-bracket` | shell v5.4 · data 2026-07-07 | PRs #76/#77. Data-separated. **⚠️ Its refresh ROUTINE is retired (tournament ended Jul 19) — the APP is not. Still live, do not retire it.** |
| `f1-results` | ⚠️ **NOT AN APP** (tombstone) | Kept so the phantom isn't re-added: this is f1-racetracks' **nested data store**, never a root app. |
| `filemaker` | unverified (reference) | FileMaker app docs; runs in FMP, not on Pages. Verify on next touch. |

**Non-app infra (never PR as an app):** `brain-config`, `agent-reports`, `shared`, `quickfire`, `template-app`.

- **`template-app` — gold standard, v5 (PR #300). Access: GATED, code `0426`.** The copy/place/audit baseline: slim hash-router `index.html` + `chrome.js` + `pages/` partials, styled off the `shared/themes` spine. Carries `CONFORMANCE.md` as the audit checklist. ⚠️ **Its `chrome.js` (10,617 B) is the FULL app chrome** — header, left nav drawer, right settings drawer with a live theme picker, JS-written footer stamp — and it has **no embed/iframe awareness**. ⚠️ **It is a COPY BASELINE, not a shared runtime: every app that copies it owns a fork.** Place it UNFORKED and let everything app-specific ride in through `cfg`; if you need something it cannot express, **change the template rather than special-casing your copy** — and that is Michael's call, not yours.
- **`shared/themes` — the 4-vector theme spine.** A THEME is a JOIN in `_themes.json` binding one row each from `colors.tsv` / `typography.tsv` / `forms.tsv` / `spacing.tsv`. 🔴 **`applyTheme()` takes a JOIN slug; the `data-theme` DOM attribute takes a COLOR slug.** Passing a color to `applyTheme()` faults — the trap that bit both `inciardi-collection` and `f1-racetracks`. Since PR #502 an unresolved pointer paints a red banner instead of half-theming in silence. **🔴 HEX IN THE TSVs IS CANONICAL — `resolve.js` reads ONLY the four TSVs + `_themes.json`. It never opens a per-colour JSON**, so `maw-dark-utility.json`, `paddock.json` and `f1/*.json` are PROJECTIONS for FileMaker and the preview lab; on a disagreement the TSV wins. `themes.css` is GENERATED and was last built 2026-07-16, so it lacks blocks for `carbon`, `eos` and `paddock`. **08-01 (PR #696):** new `paddock` colour entity + the `f1-racetracks` join, and **one hue family per team** — see `_index.json` → `accentBoardRule`.

---

## Coverage rule

**Every folder in the repo root is either in the table above or in the not-apps line. Nothing gets to be invisible.** Four apps were found in the root tree on 2026-07-25 in NEITHER former index, and a **fifth (`inciardi-collection`) on 2026-07-26 missing from this ledger itself.** An app nobody indexes is an app nobody verifies.

⚠️ **Coverage is necessary but not sufficient. It proves an app is LISTED, never that it is still NEEDED.** `routines` was correctly indexed, freshly versioned and actively maintained on the day it was deleted as redundant. **Every duplicate-check we own runs at creation time; none re-run when the world changes.**

⚠️ **Nor does coverage prove a row is TRUE, or CURRENT.** Both actively-developed apps in this repo have now been found multiple versions stale within days of being hand-corrected. **A maintained row is not a verified row** — stamp what you checked and when.

## Changelog

- **2026-08-02 — `inciardi-collection` v19 → v23** (PRs #689 + #692), and the row's *"worker is two files"* claim corrected to **six**. **Durable findings:** *a STRUCTURAL claim rots exactly like a version number and is more dangerous, because it reads as architecture rather than as state — this one carried a "split before adding any route" instruction aimed at a split that had already happened twice* · **and the row that was hand-corrected on 07-31 was stale again inside 24 hours, which retires "be more diligent at step 3" as a remedy: step 1 has now caught four gaps and step 3 has caught none.**
- **2026-08-01 — `shared/themes`: `paddock` colour entity + the `f1-racetracks` join; the F1 accent board reworked** (PR #696). **Durable findings:** *a value can be individually correct and collectively unreadable — four constructor accents sat within **1.1 hue degrees and 0.011 lightness**, two byte-identical, because `accent` is a SINGULAR-use token and a standings matrix is a PLURAL use* · *when a ruling lands as a local `swap: true` inside one app instead of as a row in the shared grid, the ruling did not land — it became an exception* · **and the one that generalizes furthest: a shared grid has no notification mechanism, so editing `colors.tsv` silently restyled `on-track`, whose maintainers were not in the room. Leverage and blast radius are the same property.**
- **2026-08-01 — `f1-racetracks` v11 → v19.1** (PRs #664 → #690). Seat/team vector, the colour vector switch, one chrome renderer, the forms vector, the token spine. **Durable findings:** *"load it last so it wins the cascade" is FALSE against modules that inject CSS at runtime — five of them here, so an override layer needed `!important` to be heard at all* · *a code search finds DECLARATIONS and proves nothing about whether your rule RENDERS; **for a CSS override the artifact is the computed value, not the declaration*** · *nobody chose the app's corner radii — one author never typed one and another did, and two defaults read months later as a design decision* · *four copies of a markup block plus two "hide it over there" rules is a defect generator, not a discipline problem.*
- **2026-08-01 — `f1-racetracks` v6.7.1 → v11** (PR #654). Season data split into vectors; every time-shaped value derived. **Durable findings:** *a store that can only contain things that ALREADY HAPPENED cannot be the calendar* · *a hand-maintained ordinal survives a normal season and dies on the first mid-season change* · *the README held the CORRECT calendar while both data stores held the wrong one — "open the JSON before you believe the plan" needs a return direction, and nothing in that app ever compared the two.*
- **2026-08-01 — `inciardi-collection` → v19** (PRs #637 + #638). **Durable findings:** *a conditional answer whose condition is a build step needs that step written down AS a step* · *a split commit is where a quiet behavioural edit hides.*
- **2026-07-31 — `inciardi-collection` v3 → v17.** **Durable finding: validation proves a payload is WELL FORMED and can say nothing about whether it is TRUE** — a confirmation screen must render in the units of the physical thing, and a screen you cannot correct is a quiz.
- **2026-07-30 — write key baked into the public bundle.** Durable findings: *a dashboard list is a UI, not the truth*; *giving a setting a default silently kills the check that tested for its absence.*
- **2026-07-28 — `f1-racetracks` → v6.7.1.** Durable finding: the open-items clause above, and *"a maintained row is not a verified row."*
- **2026-07-27 — `routines` DELETED.** Durable finding: *two claimants on one truth can be created by a capability change rather than by anyone's mistake.*
- **2026-07-26 — target reset to ~16KB.** Durable finding: *a stated target never met is the defect, not the aspiration.*
- **2026-07-25 — COLLAPSED TO ONE LEDGER** (Michael's call, Dev Dexter). `app-index.md` retired to a stub.
- Earlier history: git log + PR descriptions. Per-app history: that app's `README.md` + `next-build-spec.md`.

_`unverified` rows carry the last value visible without a fresh read; each gets confirmed the next time that app is touched. Confirmed 2026-07-25: agentglass, prism, app-dashboard, markdown-viewer, world-cup-bracket, on-track, inciardi-market. Confirmed 2026-08-01: f1-racetracks at v19.1 (`source/chrome-tokens.css` whole, both `chrome.js` files, measured byte sizes of every file in `source/`). **Confirmed 2026-08-02: inciardi-collection at v23** — `index.html` read whole at HEAD, `?v=23` on all asset tags, worker file count and module map taken from its own measured listing._
