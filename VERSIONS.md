# App Ledger — THE single source of truth for every app

**One file. One row per app. Every folder in the repo root appears here or in the not-apps line.** `brain-config/app-index.md` is retired to a stub; there is no second index.

**Also the tiebreaker against stale reads and CDN lag.** When your read disagrees with this file, **re-read at HEAD and reconcile** — the gate cuts both ways (below).

## 📏 Size (target **~16KB**, ceiling **22KB** — LOCKED 2026-07-26, Michael)

**Each row is CURRENT STATE, not version history.** Per-version narrative lives in git history + the PR description + that app's own `README.md`/`next-build-spec.md`. **This ledger cites the PR and stops.**

- **Ceiling ~22KB.** Past it the file cannot be read whole, and **a file that cannot be read whole cannot be safely edited.** ⚠️ **08-03: this was called "physics" and it is not.** `maw-agents/uritp-docs` J5 measured 30.6KB reading whole and 34.9KB clipping; the range between is uncharacterised. It is a BUDGET. Do not quote physics without a measurement.
- **Target ~16KB** = floor + real headroom; it replaced a ~12KB target never once met (Floor Rule, `brain-config/hooks/source-size-budget-enforcer.md`).
- **Never split the table** — trim prose. Seat Size Sally before this grows.
- 🔴 **EVERY EDIT THAT ADDS MUST ALSO TRIM, IN THE SAME COMMIT, AND READ THE SIZE BACK.** Six instances; the last two (24,140 B on 08-02, 26,843 B on 08-03) were shipped by sessions that had just read this bullet. **Growth is always prose, never rows, and intending to trim is not trimming.** If your edit does not delete something concrete, it is not finished.

## Procedure (MANDATORY on every app PR)

1. **Before touching, editing, PR-ing, or even DISCUSSING an app:** read this file. Compare against the version you *think* is current.
2. **Mismatch = STOP.** Re-read at an immutable commit-SHA path or the git blob API (base64-decoded) — never a branch raw URL, which serves copies many versions stale and flattens HTML/SVG out of template literals. Reconcile before building.
3. **Immediately after opening a PR:** update that app's row in the same session. The ledger must never lag `main`.
4. Advisory-blocking on discussion, hard-blocking on edits.

⚠️ **The gate cuts BOTH ways — this file is a CLAIM about reality, not reality. HEAD wins; this file gets corrected.** Four measured rot classes: a **WARNING** (07-25, a "restore pending" note 32 PRs stale, nearly triggering a destructive revert) · an **OPEN-ITEM** (07-28 — *a done job in an open list is an instruction to redo it*) · a **CLASSIFICATION** (07-30) · and a **STRUCTURAL CLAIM** (08-02 "the worker is two files" when it was six; 08-03 a file size asserted under a split line it had crossed) — **the worst class, because it reads as architecture rather than as state.** **Verify before acting on any ⚠️.**

🔴 **A ROW GOES STALE BY DEFAULT, NOT BY ACCIDENT — SIX MEASUREMENTS IN FOUR DAYS.** `inciardi-collection` v3-vs-v16 (07-31), `f1-racetracks` eight versions behind (08-01), `inciardi-collection` four behind *again* 24 hours after a hand correction (08-02), `prism` at v1 against a documented v2.2 (08-03), `git-grab` size claim false within one day (08-03).

> ⭐ **"Try harder at step 3" is retired as a remedy.** Step 3 fires at the END of work, when the thing you came to do is already done, so it has the least momentum behind it. **Step 1 is the enforcement mechanism — read the ledger on the way IN and the gap is unmissable.** Every gap above was caught by step 1; none by step 3.

**Data-separated apps carry TWO version facts:** the *shell* build and the *data* date. A data refresh does NOT bump the shell.

**Version-stamp convention:** carry a version the app itself declares (`APP_VERSION`, or the `?v=` cache-bust) — **not a PR number, not the word "live."** ⚠️ **When an app declares its version in more than one place, say WHICH ONE this row quotes and name the others as disagreeing.** ⚠️ **Never write a version — or a byte count — you have not READ BACK at HEAD.**

---

## Ledger

Live URL pattern: `https://mawizorek.github.io/ClickUp_apps/<slug>/`. Unmarked rows are live.

| App (slug) | Version | State + live warnings (detail → PR + app README) |
| --- | --- | --- |
| `agentglass` | shell **v2** · seed 2026-07-25 · **SNAPSHOT** | PR #488. Agent Activity Board liveness feed. Hash-router shell + `pages/` + `source/`; non-executing `server/` tree. **⚠️ NOT PASSIVE YET** — reads a repo-committed seed; the header pill reads `live` vs `snapshot` so it cannot imply false currency. To go live: deploy `server/worker.ts`, set `LIVE_URL`, bump `?v=`. Capture is 3 labeled tiers; derived rows are NEVER billable. **Do NOT fence-match ClickUp comments (brace-match only) or add latency percentiles (J3).** Telemetry is self-attested, not an audit trail. |
| `app-dashboard` | **v4** (`?v=73`) | PR #72/#74. Slim launcher; thin `index.html` loader over `source/*`. Live tree via GitHub API, health dots, 5-min cache, ClickUp/FileMaker toggle, Show-retired toggle, detail sheet. **Bump `?v=` on every source change.** `supersededBy` in `data.js` must name another APP slug. ⚠️ **Do NOT revert PR #59/#57** — stale by 32 PRs; reverting destroys current work (verified at HEAD 07-25, PR #485). |
| `budget-code-mapper` | unverified (last known v6, design) | Verify + stamp a real version on next touch. |
| `cast-grid` | **v0.1-poc** | POC. Roles × Productions matrix, live from ClickUp API. Client-side, token-prompted, field-mapping UI. Proving relationship field resolution + pivot rendering. |
| `f1-racetracks` | shell **v19.1** (circuits `?v=20260801e`, standings + weekend `?v=21`, `app-shell.js` `?v=11`) · **23-round calendar 2026-08-01** · results store verified `d27ce55` (r1-9) | PRs #654 → **#690**. ⚠️ **This row said v11 until 08-01 — eight versions behind.** **🔴 THE DATA LAYER IS FOUR VECTORS — read the app README ▸ Data Model AND Decision Log J10-J21 before touching any JSON.** CIRCUITS (`circuits/<slug>.json`, timeless) · **WEEKENDS (`season/2026/index_weekends.json` — THIS is the season calendar)** · **SEATS+TEAMS (`season/2026/index_drivers.json`)** · CLASSIFICATIONS (`f1-results/2026/`, only rounds actually raced). **NOTHING TIME-SHAPED IS STORED** — round, status, display date and the round pointers are derived at boot by `source/08_season.js`, so a cancellation or insertion self-heals. **🚨 NEVER PERSIST A DERIVED ORDINAL — join by `slug` or `cuTaskId`, never by round number** (a mid-season renumber left every entry from Miami on off by two for eight months). **Sepang** hosts the *Bahrain* GP, 2-4 Oct; its circuit file is MINIMAL + sourced-only + `report:false`. **Team colour reads `shared/themes/colors.tsv` at boot, injected as `--t-<team>` (v16)** — a LOCAL IDENTITY LAYER, never a theme vector: a theme applies one at a time, the matrix renders eleven teams at once. 🔴 **FIVE VERSION CLAIMANTS, ALL DISAGREEING** (`source/09` v12 · `standings/data.js` v6.0 · `circuits.html` v19.1 · `README.md` v11 · this row, which quotes the shell). **The fix is the v20 port** — one `config.json`, footer written from it — NOT five hand-edits: `source/09` is **29,446 B** and `README.md` **26,076 B**, both past the safe-edit ceiling, and the port rewrites both. ⚠️ Open: **`grid`+`qualifying` absent on r03/r04/r07** (66 driver-rows, SOURCED or absent — do not improvise), sprint arc documented-never-built, sprint `sess` labels disagree with the authoritative boolean on 5 circuits, no `sepang` in `LIVE_CIRCUIT_ALIASES`, FOUR source modules over the 15KB line, `chrome-tokens.css` §8 is an `!important` override layer whose exit condition is the port. **`live-tracker.html` is standalone BY DESIGN** (Michael 08-01) — no spine, will drift, **not a defect**. ✅ **`applyTheme('f1-racetracks')` NOW RESOLVES** (PR #696). **The app does not call it yet; that is the port.** 🅿️ **v20 PORT PARKED ON `f1-racetracks-v20-port`** — 3 commits, no PR, `config.json` + `chrome.js` placed UNFORKED. **Do not re-cut that branch.** ⚠️ **RICKY:** `cuTaskId` lives on the weekend row, `current_round_slug` is gone; adding a round is the round file + one `{slug,file}` row. |
| `file-chunker` | unverified (last known v16.2) | Generates the `/source` chunk sets for over-cap apps. Verify on next touch. |
| `git-grab` | **v0.2** (`APP_VERSION` + `?v=2`, read back at HEAD) · 🔒 **GATED** (code `0426`) · **STILL WAVE 0 — NO APP LOGIC** | PRs #713 → **#714**. Paste a public GitHub folder URL → zip of that folder, recursively. Replaces `download-directory.github.io` on an origin Michael controls (an audit of a mutable origin does not bind its next deploy). Placed from `template-app` v5. Theme join **`soft-utility`**, colour fallback `maw-dark-utility`; zero new vectors applied, zero new objects, zero colour literals. **🔴 THE CORRECTNESS BAR IS THE WHOLE APP: a silently short zip is the only failure that matters.** Acceptance = *count on screen == entries in zip == count from GitHub, exclusions named.* **Tree modes `160000` (submodule/gitlink, no blob exists) and `120000` (symlink, blob is the link target as text) MUST be filtered AND named** — dropping either quietly IS the failure. `truncated: true` on the trees response means **refuse**, never a partial zip. ⚠️ **Not yet built: `zip.js` (Wave 1), `gh.js` (Wave 2), `app.js` (Wave 3).** Wave plan in `git-grab/next-build-spec.md`. 🔴 **v0.2 — THE LAYOUT RAMP IS FLOORED, AND THE REASON GENERALIZES TO EVERY TEMPLATE COPY:** v0.1 rendered with no page or header gutter while cards kept their padding, because **an unresolved `var()` invalidates the WHOLE declaration and reverts it to the CSS initial value** — `padding` becomes `0`, it does not degrade. Rules spelling their own fallback (`var(--pad-card, 20px)`) survived; the bare `--s*` ramp did not. Every ramp step now carries a fallback and `--gutter: max(var(--s4), 16px)` floors the three page-level gutters. **The bare ramp is still live in `template-app/styles.css`.** ⚠️ **`styles.css` is 15,892 B — OVER the 15KB split line** (this row said 14,130 B and "under" on 08-03; false within a day). It is a **fork**, not an unforked copy. ⚠️ **`og.png` does not exist**; the head tags point at it. |
| `inbox-digest-report` | v6 (shell) · data Jul 17 2026 | PR #323. Gmail Inbox Sweep audit surface. **PURE RENDERER of `data/inbox-state.json`** — the sweep edits ONLY the JSON. Field spec in `data/README.md`. |
| `inciardi-collection` | **v23** (`?v=23`, read back from `index.html` at HEAD) · workers deployed · theme `soft-mercedes` · D1 live | PRs #590→**#692**. ⚠️ **THIS ROW WENT STALE TWICE IN TWO DAYS** (v3-vs-v16 on 07-31, v19-vs-v23 on 08-02), both caught on the way IN. **READ THIS FILE BEFORE TOUCHING THE APP.** Digital twin of the mini-print binder: binder → sheets → sides → 9 slots. **Its OWN worker + D1, separate from `inciardi-market`.** Thin `index.html` router + JS modules + `pages/` partials. **🔴 THE WORKER IS SIX FILES** — `worker.js` (gate + dispatch) · `reads.js` (GET routes) · `images.js` (upload/serve/carousel) · `links.js` (attach/bring-to-front/archive) · `adopt.js` (the legacy 177 import) · `sheets.js` (binder mechanics). **Sizes for all six are measured in `index.html`'s module map — re-measure there, never estimate.** **🔴 v22 — ONE ORDERING, ONE OWNER:** `photoOrder()` lives once in `links.js`; `reads.js` (card, `LIMIT 1`) and `images.js` (carousel, no limit) both import it, so the card photo IS the carousel's first frame structurally. `is_primary` is **retired from the ranking** for `edition_image.sort` — starring writes `MIN(sort)-1`, undone by starring something else. ⚠️ **The column still exists and is meaningless; it goes with migration 002.** **🔴 v23 — the print page has its photographs:** `photoview.js` is a module with **no page of its own**, so `photos.js` can call the same component — ⚠️ **it DOES NOT call it yet**, a named seam, not an oversight. 🔴 **PINCH-ZOOM IS DEFENDED THREE WAYS** in `photoview.js` (swipe abandons on a 2nd touch point · `touch-action: pan-y pinch-zoom` · `gesturestart` untouched). **Never add `maximum-scale` or `user-scalable=no`** — it breaks a shipped feature. **ROUTES:** binder · summary · shoebox · enter · photos · **`#artwork?id=`** (detail, in no menu) · `#backroom` (unlisted). **BACK ROOM** bulk-imports from `batches/<slug>.json` — **data, never code**; its 3×3 grid is editable and **the editor writes the RUN, not the FILE** — commit corrected grids back. **🔴 THE WRITE KEY SHIPS PUBLICLY, ON PURPOSE** — `core.js` → `DEFAULT_KEY` **and** `wrangler.toml` → `[vars] WRITE_KEY`, Michael's call. **The pair must match; rotate both or neither.** A mismatch is a **401 on first save, never on `/health`**. ⚠️ **Bare worker URLs have served 24-hour-stale reads**; add `?nocache=`. ⚠️ `README.md` still claims the key is never bundled. ⚠️ **Several front-end files sit over the 15KB split line and `photoview.js` shipped over it on day one** — the ⚠️ rows and their named seams are in the module map. **Do NOT restore a read-only banner in `checkConfig()`, an `ic_color` read, or the back room's number strip.** |
| `inciardi-market` | front-end **v16** · both workers v1.4 · 🪦 **BEING RETIRED** | PR #474. Two Cloudflare workers over one D1+R2 store; **NOT data-separated**. **Michael, 07-31: "consider that app dead."** 🔴 **DO NOT DELETE ITS D1 YET.** `print_image` holds the only R2-key → which-print mapping for the 177 images in `inciardi-images` — **export that join before any teardown** or 268MB becomes anonymous objects. **🔴 OPEN SECURITY: `LOGINS` holds guessable write keys in the public bundle** — distinct from the *deliberate* baked key next door: readable is a tradeoff, **guessable is a defect**. **Image Rendering Law:** `proxied(url,360)` is a no-op on an R2 `/img?key=` URL. Prices are DOLLARS not cents. |
| `markdown-viewer` | **RETIRED** | Superseded by `prism`. Hidden behind Show-retired. **Do not develop.** |
| `on-track` | shell **v2.4** (`?v=24`) · data 2026-07-23c | PRs #463/#468/#470. Data-separated. 2nd theme-spine consumer: composed 4-vector theme, **passive Theme picker** reading `_themes.json` live. **The reference implementation for a theme picker.** `:root` = labeled first-paint FALLBACK FLOOR. **The 20 series colors are a LOCAL data layer.** ⚠️ **08-01: its join points at the `red-bull` COLOR row, whose accent moved red → yellow in PR #696** (four teams shared one red). **Its chrome accent changed and nobody from this app was in the room.** For the old red: `accent-2` on the same row, or its own colour entity — **not a local override.** 🐞 **Open bug:** favicon/apple-touch/og:image point at `IMG_*.png` files that are GONE. One-line fix. |
| `pdf-splitter` | unverified (last known v1) | Verify + stamp on next touch. |
| `polish-demo` | demo | Working example of the pro-polish standard. Gate code `2026`. Not a launchable app. |
| `prism` | 🔴 **DISPUTED — this row says v1, the app documents v2.2** | PR #54 (v1). Data App Viewer (JSON + Markdown). Modular: slim `index.html` + `prism.css` + four JS modules. Gate code `2026`, currently `open`. Folds in the retired `markdown-viewer`. ⚠️ **08-03: `prism/README.md` at HEAD documents v2, v2.1 and v2.2 — this row is at least three versions stale.** **NOT reconciled on purpose:** never write a version you have not read back, and the authoritative stamp is the `?v=` token in `prism/index.html`, which was not read. **Next agent to touch prism: read that token and stamp this row.** |
| `report-normalizer` | ⚠️ **unverified, never indexed** | Found 2026-07-25, absent from BOTH former indexes. Needs a version stamp + a real row on next touch. |
| `retrocast` | ⚠️ **unverified, never indexed** | Found 2026-07-25 — despite being the **FIRST theme-spine consumer** per this ledger's own rollout note. Needs a version stamp. |
| `routines` | 🗑️ **DELETED 2026-07-27 — NO LONGER AN APP** | PR #562. The folder survives as **docs only** and is listed purely so the app is not re-added. **Do not rebuild it.** **Its lesson lives in `routines/next-build-spec.md`: the app never rotted — retiring the scheduler is what turned it into a duplicate. When a capability is retired, ask what existed only to compensate for it.** |
| `squash` | ⚠️ **unverified, never indexed** | Found 2026-07-25. Needs a version stamp + a real row. |
| `Vectorworks` | ⚠️ **unverified, never indexed** | Found 2026-07-25. Also the ONLY capitalized folder in a kebab-case repo — needs a naming decision alongside its stamp. |
| `world-cup-bracket` | shell v5.4 · data 2026-07-07 | PRs #76/#77. Data-separated. **⚠️ Its refresh ROUTINE is retired (tournament ended Jul 19) — the APP is not. Still live, do not retire it.** |
| `f1-results` | ⚠️ **NOT AN APP** (tombstone) | Kept so the phantom isn't re-added: this is f1-racetracks' **nested data store**, never a root app. |
| `filemaker` | unverified (reference) | FileMaker app docs; runs in FMP, not on Pages. Verify on next touch. |

**Non-app infra (never PR as an app):** `brain-config`, `agent-reports`, `shared`, `quickfire`, `template-app`.

- **`template-app` — gold standard, v5 (PR #300). Access: GATED, code `0426`.** The copy/place/audit baseline: slim hash-router `index.html` + `chrome.js` + `pages/` partials, styled off the `shared/themes` spine. Carries `CONFORMANCE.md` as the audit checklist. ⚠️ **Its `chrome.js` (10,617 B) is the FULL app chrome**, with **no embed/iframe awareness**. ⚠️ **It is a COPY BASELINE, not a shared runtime: every app that copies it owns a fork, so a fix in one copy reaches no other app.** Place it UNFORKED; if you need something it cannot express, **change the template** — Michael's call. 🐞 **THREE OPEN DEFECTS, all found 08-03 by `git-grab`, all one-line fixes at the source and all Michael's call:** (1) `styles.css` contains the literal `--gap-*/--pad-*` inside a CSS comment — **the `*/` terminates the comment early and leaks the rest of the sentence into the stylesheet as garbage**; (2) `CONFORMANCE.md`'s theme line (*"Default is `default-theme`"*) **reads as a JOIN instruction but states a COLOUR fact** — `default-theme` has no join row, so following it literally faults; (3) **the `--s1`…`--s8` layout ramp is written with bare `var()` and no fallbacks**, so one unresolved spacing token silently zeroes every page and header gutter in every app copied from it. |
- **`shared/themes` — the 4-vector theme spine.** A THEME is a JOIN in `_themes.json` binding one row each from `colors.tsv` / `typography.tsv` / `forms.tsv` / `spacing.tsv`. 🔴 **`applyTheme()` takes a JOIN slug; the `data-theme` DOM attribute takes a COLOR slug.** Passing a color to `applyTheme()` faults — it bit `inciardi-collection` and `f1-racetracks`, and `git-grab` nearly shipped it on 08-03 because its own reviewed plan named a colour. Since PR #502 an unresolved pointer paints a red banner instead of half-theming in silence. **🔴 HEX IN THE TSVs IS CANONICAL — `resolve.js` reads ONLY the four TSVs + `_themes.json`, never a per-colour JSON**, so `paddock.json` and `f1/*.json` are PROJECTIONS; on a disagreement the TSV wins. `themes.css` is GENERATED, last built 2026-07-16, so it lacks blocks for `carbon`, `eos` and `paddock`. **08-01 (PR #696):** the `paddock` colour entity + the `f1-racetracks` join, and **one hue family per team** — see `_index.json` → `accentBoardRule`. **08-03 (PR #714):** new **`flush`** spacing row (dense/gutterless, Michael's "database" density). **ADDITIVE — no join points at it, so it restyles nothing**; that is the difference between it and the 08-01 `colors.tsv` edit, which reached an app whose maintainers were not in the room. ⚠️ **08-03, unresolved, steward's call:** the token COUNT disagrees across three files (`_index.json` **17** `tokenKeys` · `OBJECT-COVERAGE.md` **22** colours · `CONFORMANCE.md` *"the 17-token contract"*), and 🐞 **`resolve.js` → `applyVector()` faults on a missing ROW but never on a BLANK CELL**, so a half-filled vector row applies a half theme in silence — inside the file whose header is a manifesto against silent fallbacks. |

---

## Coverage rule

**Every folder in the repo root is either in the table above or in the not-apps line. Nothing gets to be invisible.** Four apps were found in the root tree on 07-25 in NEITHER former index, and a fifth (`inciardi-collection`) on 07-26 missing from this ledger itself. **An app nobody indexes is an app nobody verifies.**

⚠️ **Coverage proves an app is LISTED — never that it is still NEEDED, never that the row is TRUE.** `routines` was correctly indexed, freshly versioned and actively maintained on the day it was deleted as redundant: **every duplicate-check we own runs at creation time and none re-run when the world changes.** **A maintained row is not a verified row.**

## Changelog

**Findings live in the PR descriptions; this list cites and stops.** One line per change — see the size rule above for why.

- **08-03 — `git-grab` v0.1 → v0.2** (PRs #713, #714): Wave 0 scaffold, then spacing floor + saved-URL dropdown + gated; `flush` spacing row added; `prism` flagged DISPUTED; four defects logged in shared standards. *Two keepers: a reviewed written plan is not evidence — this build's own plan named a COLOUR as its theme JOIN and would have faulted. And an unresolved `var()` does not degrade a declaration, it deletes it.*
- **08-02 — `cast-grid` v0.1-poc added.**
- **08-02 — `inciardi-collection` v19 → v23** (PRs #689, #692); "worker is two files" corrected to six. *A structural claim rots like a version number and is worse, because it reads as architecture.*
- **08-01 — `shared/themes`: `paddock` entity + `f1-racetracks` join; F1 accent board reworked** (PR #696). *A shared grid has no notification mechanism — editing `colors.tsv` silently restyled `on-track`, whose maintainers were not in the room. Leverage and blast radius are the same property.*
- **08-01 — `f1-racetracks` v11 → v19.1** (PRs #664 → #690). *For a CSS override the artifact is the COMPUTED value, not the declaration.*
- **08-01 — `f1-racetracks` v6.7.1 → v11** (PR #654). *A store that can only contain what ALREADY HAPPENED cannot be the calendar.*
- **08-01 — `inciardi-collection` → v19** (PRs #637, #638). *A split commit is where a quiet behavioural edit hides.*
- **07-31 — `inciardi-collection` v3 → v17.** *Validation proves a payload is WELL FORMED, never that it is TRUE.*
- **07-30 — write key baked into the public bundle.** *Giving a setting a default silently kills the check that tested for its absence.*
- **07-27 — `routines` DELETED.** *Two claimants on one truth can be created by a capability change rather than anyone's mistake.*
- **07-26 — target reset to ~16KB.** *A stated target never met is the defect, not the aspiration.*
- **07-25 — COLLAPSED TO ONE LEDGER** (Michael's call, Dev Dexter). `app-index.md` retired to a stub.
- Earlier history: git log + PR descriptions. Per-app history: that app's `README.md` + `next-build-spec.md`.

_`unverified` rows carry the last value visible without a fresh read; each gets confirmed the next time that app is touched. Confirmed 07-25: agentglass, app-dashboard, markdown-viewer, world-cup-bracket, on-track, inciardi-market. 08-01: f1-racetracks at v19.1. 08-02: inciardi-collection at v23. **08-03: git-grab at v0.2 from its own `APP_VERSION`, with `styles.css` at 15,892 B read back from the write response; prism's 07-25 confirmation WITHDRAWN — it confirmed v1 and the app was already past it.**_
