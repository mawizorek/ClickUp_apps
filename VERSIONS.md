# App Ledger — THE single source of truth for every app

**One file. One row per app. Every folder in the repo root appears here or on the not-apps line.** `brain-config/app-index.md` is retired to a stub; there is no second index.

📏 **The rules, the rot taxonomy and the history live in [`brain-config/ledger-standard.md`](brain-config/ledger-standard.md).** Read it once. This file is the data you read every time.

## Procedure (MANDATORY on every app PR)

1. **Before touching, editing, PR-ing, or even DISCUSSING an app:** read this file. Compare against the version you *think* is current.
2. **Mismatch = STOP.** Re-read at an immutable commit-SHA path or the git blob API (base64-decoded) — never a branch raw URL, which serves copies many versions stale and flattens HTML/SVG out of template literals. Reconcile before building.
3. **Immediately after opening a PR:** update that app's row in the same session. The ledger must never lag `main`.
4. Advisory-blocking on discussion, hard-blocking on edits.

⚠️ **The gate cuts BOTH ways — this file is a CLAIM about reality, not reality. HEAD wins; this file gets corrected.** Verify before acting on any ⚠️ below; every class of claim here has been measured going stale (taxonomy → the standard).

⚠️ **Never write a version, a byte count, or a claim ABOUT a set of files, that you have not READ BACK at HEAD.**

📐 **Size: target ~16KB, ceiling ~22KB.** Every edit that adds must also trim, in the same commit, and read the size back. Seat Size Sally before growing this file. 🔴 **MEASURED 2026-09-21: 22,363 B inflated to 29,820 base64 chars against a ~30,720 return cap — ~675 B from UNREADABLE, and unreadable means unrewritable. Record: PR #952.**

⚠️ **A blob-API read CLIPPED a 16,829 B file on 08-03** (base64 inflates 4/3 against the return cap). Near the line, cross-verify a second read path on the overlap; never rewrite from a truncated read.

🅿️ **BOTH PARKED ROW-MOVES TAKEN 2026-09-21** (PR #952), plus the two infra bullets condensed. ⚠️ **The standard's *"detail belongs in the app README"* is UNFOLLOWABLE for both: those READMEs are 26,076 B and 23,389 B, themselves past safe-edit.** Detail went to `next-build-spec.md` / `DEPLOY.md` instead.

---

## Ledger

Live URL pattern: `https://mawizorek.github.io/ClickUp_apps/<slug>/`. Unmarked rows are live.

| App (slug) | Version | State + live warnings (detail → PR + app README) |
| --- | --- | --- |
| `agent-load` | ⚠️ **SPEC ONLY — nothing built** | PR #951. Passive agent-load report: pure renderer over a committed **aggregates** JSON, fed by a session-fired hook. Theme join **`eos`** (4 vectors verified populated); 🔴 **no `eos` block in `themes.css` — MUST resolve at runtime.** Contract + rationale: `agent-load/`. |
| `agentglass` | shell **v2** · seed 2026-07-25 · **SNAPSHOT** | PR #488. Agent Activity Board liveness feed. Hash-router shell + `pages/` + `source/`; non-executing `server/` tree. **⚠️ NOT PASSIVE YET** — reads a repo-committed seed; the header pill reads `live` vs `snapshot` so it cannot imply false currency. To go live: deploy `server/worker.ts`, set `LIVE_URL`, bump `?v=`. Capture is 3 labeled tiers; derived rows are NEVER billable. **Do NOT fence-match ClickUp comments (brace-match only) or add latency percentiles (J3).** Telemetry is self-attested, not an audit trail. |
| `app-dashboard` | **v4** (`?v=73`) | PR #72/#74. Slim launcher; thin `index.html` loader over `source/*`. Live tree via GitHub API, health dots, 5-min cache, ClickUp/FileMaker toggle, Show-retired toggle, detail sheet. **Bump `?v=` on every source change.** `supersededBy` in `data.js` must name another APP slug. ⚠️ **Do NOT revert PR #59/#57** — stale by 32 PRs; reverting destroys current work (verified at HEAD 07-25, PR #485). |
| `audio-compressor` | **v1**, live | Client-side audio/video → MP3 compressor via **ffmpeg.wasm** (CDN at runtime), 32-128 kbps, mono/stereo. **No upload, no server.** Built for the 25MB transcription cap; `devising-transcript-archive` names it as the compress step. Self-contained, under the 30KB cap. Detail: `audio-compressor/README.md`. |
| `budget-code-mapper` | unverified (last known v6, design) | Verify + stamp a real version on next touch. |
| `cast-grid` | **v0.1-poc** | POC. Roles × Productions matrix, live from ClickUp API. Client-side, token-prompted, field-mapping UI. Proves relationship resolution + pivot rendering. |
| `doc-sandbox` | **v1.1** (`app.css?v=1` · `source/app.js?v=2`) | Renders and prints ONE doc-render page locally with the real published look. Thin shell + `source/*.js`; theme join **`soft-utility`**. 🔴 **The preview IFRAME is deliberate** — Material's sheets style `body`/`:root` and would restyle this app's own chrome; it also makes print output the preview alone. ⚠️ **A module does not inherit its parent's query string** (the `sites.js` bump lives in `app.js`). Spec: `doc-sandbox/next-build-spec.md`. |
| `f1-racetracks` | shell **v19.1** (circuits `?v=20260801e`, standings + weekend `?v=21`, `app-shell.js` `?v=11`) · **23-round calendar 2026-08-01** · results store verified `d27ce55` (r1-9) | PRs #654 → **#690**. **🔴 THE DATA LAYER IS FOUR VECTORS — read the app README ▸ Data Model AND Decision Log J10-J21 before touching any JSON.** CIRCUITS (`circuits/<slug>.json`, timeless) · **WEEKENDS (`season/2026/index_weekends.json` — THIS is the season calendar)** · **SEATS+TEAMS (`season/2026/index_drivers.json`)** · CLASSIFICATIONS (`f1-results/2026/`, only rounds actually raced). **NOTHING TIME-SHAPED IS STORED** — round, status, display date and the round pointers are derived at boot by `source/08_season.js`, so a cancellation or insertion self-heals. **🚨 NEVER PERSIST A DERIVED ORDINAL — join by `slug` or `cuTaskId`, never by round number** (a mid-season renumber left every entry from Miami on off by two for eight months). **Team colour reads `shared/themes/colors.tsv` at boot, injected as `--t-<team>` (v16)** — a LOCAL IDENTITY LAYER, never a theme vector. 📋 **FIVE DISAGREEING VERSION CLAIMANTS, THE OPEN-DEFECT INVENTORY, THE SEPANG/BAHRAIN NOTE AND THE v20 PORT PLAN → [`f1-racetracks/next-build-spec.md`](f1-racetracks/next-build-spec.md) § Ledger detail (moved 2026-09-21). READ IT FIRST.** **`live-tracker.html` is standalone BY DESIGN** (Michael 08-01) — will drift, **not a defect**. ✅ **`applyTheme('f1-racetracks')` NOW RESOLVES** (PR #696); **the app does not call it yet — that is the port.** 🅿️ **v20 PORT PARKED ON `f1-racetracks-v20-port`** — 3 commits, no PR. **Do not re-cut that branch.** ⚠️ **RICKY:** `cuTaskId` lives on the weekend row; adding a round is the round file + one `{slug,file}` row. |
| `file-chunker` | unverified (last known v16.2) | Generates the `/source` chunk sets for over-cap apps. Verify on next touch. |
| `git-grab` | **v1.3** (`APP_VERSION` + `?v=7`, read back at HEAD) · access **open** · ✅ v1.2 verified end-to-end 08-03 by Michael | PRs #713 → **#745**. Public GitHub folder URL → recursive zip, built in the browser. Theme join **`soft-utility`**; **`APP_THEME` is one line and reskins everything.** Zero new vectors/objects/colour literals. **🔴 THE CORRECTNESS BAR IS THE WHOLE APP: a silently short zip is the only failure that matters.** The count is asserted TWICE (`gh.js` blobs==listed; `app.js` zip entries==listed, before the download is offered) and both numbers print on screen. **Modes `160000` (gitlink) and `120000` (symlink) are excluded AND NAMED**; `truncated:true` **refuses**. **🔴 v1.2 GENERATION COUNTER — do not remove it:** the URL field's Enter key had no busy check, so a second job could start mid-fetch and the assertion would compare one folder's zip against a different folder's listing. **🔴 v1.3 RENAME LAYER (`names.js`): `.md`→`.txt` and `index.md`→`<folder>_index`, both OFF by default, previewed in the file table before anything downloads. MARKDOWN-ONLY IS A SAFETY RULE — renaming `index.html`/`index.js` breaks the folder you just downloaded. A COLLISION SKIPS ONE RENAME AND KEEPS BOTH FILES**, never drops one, which is exactly why the count assertions are untouched by it. **🔴 THREE NAMES PER FILE — fetch by `path`, plan by `rel`, pack by `out`**; swapping them is a 404 storm. **📏 JS IS SPLIT BY LANE** (zip / gh / names / view / app), each under the 15KB line — `view.js` exists because `app.js` hit 14,841 B. **CSS is split BY ORIGIN** (`styles.css` template baseline, `app.css` this app's objects). 🧪 **The Self test page runs `NAMES.selftest()` AND `ZIP.selftest()` on one button** — the name checks are conclusive, the zip checks are NOT (nothing in a tab can prove an OS extractor accepts a file). **🔴 GUARDS: warn 100MB · confirm 250MB · refuse >1GB — POLICY LINES FROM ARITHMETIC, NOT MEASUREMENTS.** ⚠️ **No ZIP64** (>65,535 files or >4GB throws) · **no private repos** (token seam exists unused, DL Q2) · **`og.png` does not exist** though the head tags point at it. **Everything else: `git-grab/README.md`.** |
| `inbox-digest-report` | v6 (shell) · data Jul 17 2026 | PR #323. Gmail Inbox Sweep audit surface. **PURE RENDERER of `data/inbox-state.json`** — the sweep edits ONLY the JSON. Field spec in `data/README.md`. |
| `inciardi-collection` | **v23** (`?v=23`, read back from `index.html` at HEAD) · workers deployed · theme `soft-mercedes` · D1 live | PRs #590→**#692**. ⚠️ **THIS ROW WENT STALE TWICE IN TWO DAYS** (07-31, 08-02), both caught on the way IN. Digital twin of the mini-print binder: binder → sheets → sides → 9 slots. **Its OWN worker + D1, separate from `inciardi-market`.** **🔴 v22 — ONE ORDERING, ONE OWNER:** `photoOrder()` lives once in `links.js`; `reads.js` and `images.js` both import it, so the card photo IS the carousel's first frame structurally. `is_primary` is **retired from the ranking**; starring writes `MIN(sort)-1`. ⚠️ **Column still exists, meaningless; goes with migration 002.** **🔴 v23:** `photoview.js` is a module with **no page of its own** so `photos.js` can call it — ⚠️ **it DOES NOT yet**, a named seam, not an oversight. 🔴 **PINCH-ZOOM IS DEFENDED THREE WAYS** in `photoview.js`. **Never add `maximum-scale` or `user-scalable=no`** — it breaks a shipped feature. **ROUTES:** binder · summary · shoebox · enter · photos · **`#artwork?id=`** (in no menu) · `#backroom` (unlisted). **BACK ROOM** bulk-imports from `batches/<slug>.json` — **data, never code**; **the editor writes the RUN, not the FILE** — commit corrected grids back. 🔐 **WORKER FILE MAP, THE DELIBERATELY-PUBLIC WRITE-KEY PAIR, `?nocache=` AND THE STALE README CLAIM → [`inciardi-collection/DEPLOY.md`](inciardi-collection/DEPLOY.md) (moved 2026-09-21). READ IT BEFORE ANY WORKER OR KEY WORK.** **Do NOT restore a read-only banner in `checkConfig()`, an `ic_color` read, or the back room's number strip.** |
| `inciardi-market` | front-end **v16** · both workers v1.4 · 🪦 **BEING RETIRED** | PR #474. Two Cloudflare workers over one D1+R2 store; **NOT data-separated**. **Michael, 07-31: "consider that app dead."** 🔴 **DO NOT DELETE ITS D1 YET.** `print_image` holds the only R2-key → which-print mapping for the 177 images in `inciardi-images` — **export that join before any teardown** or 268MB becomes anonymous objects. **🔴 OPEN SECURITY: `LOGINS` holds guessable write keys in the public bundle** — distinct from the *deliberate* baked key next door: readable is a tradeoff, **guessable is a defect**. **Image Rendering Law:** `proxied(url,360)` is a no-op on an R2 `/img?key=` URL. Prices are DOLLARS not cents. |
| `markdown-viewer` | **RETIRED** | Superseded by `prism`. Hidden behind Show-retired. **Do not develop.** |
| `on-track` | shell **v2.4** (`?v=24`) · data 2026-07-23c | PRs #463/#468/#470. Data-separated. 2nd theme-spine consumer: composed 4-vector theme, **passive Theme picker** reading `_themes.json` live. **The reference implementation for a theme picker.** `:root` = labeled first-paint FALLBACK FLOOR. **The 20 series colors are a LOCAL data layer.** ⚠️ **08-01: its join points at the `red-bull` COLOR row, whose accent moved red → yellow in PR #696.** **Its chrome accent changed and nobody from this app was in the room.** For the old red: `accent-2` on the same row, or its own colour entity — **not a local override.** 🐞 **Open bug:** favicon/apple-touch/og:image point at `IMG_*.png` files that are GONE. |
| `pdf-splitter` | unverified (last known v1) | Verify + stamp on next touch. |
| `polish-demo` | demo | Working example of the pro-polish standard. Gate code `2026`. Not a launchable app. |
| `prism` | **v3.2** (`?v=7`, read back at HEAD) · access **open** | PRs #54 → **#727**. Data App **Workbench**: tabular files are EDITABLE, hex cells are live colour swatches, any column PINS to the left. **8 JS modules, all under the 15KB line**; 2 stylesheets, ⚠️ **`prism.css` is 17,183 B — OVER it, and it clips on a full read**, which is why table styles are a SIBLING sheet and never an append. Architecture + seams + defect repros → `prism/README.md`, read it first. 🔴 **THE LENS REGISTRY IS REAL AS OF v3 AND WAS A DOCUMENTED LIE BEFORE IT.** **`prism.json.js` / `prism.md.js` were NOT touched** — core adapts their globals; new lenses self-register onto `window.PrismLenses`. 🔴 **VIEW STATE NEVER REORDERS THE FILE** — sort and pins are display-only; `T.cols` is the export order. 🔴 **SEAM IS RENDER vs ASSEMBLE** — `prism.table.cell.js` is one cell, `prism.table.grid.js` the table around it; **cross-file calls resolve through `TB`/`TBUI` at CALL time, never hoist one into a load-time var.** ⚠️ **A flat scalar-only JSON array opens in the Table lens, not the JSON lens.** |
| `qr-forge` | **v1.1** (`?v=2`; stamp from `source/build.js`, the only claimant) · access **open** | PRs #847 → **#848**. Static QR codes built in the browser: the URL is encoded into the module pattern, so no shortener, no redirect service, no third party who can withdraw a printed code. **🔴 ENCODER IS HAND-WRITTEN ISO/IEC 18004, NOT A LIBRARY** — `qr-gf.js` (GF(256) + Reed-Solomon) + `qr-core.js` (v1-40, byte mode/UTF-8, interleaving, 8 masks + 4 penalty rules, both BCH strings). **🚫 NEVER swap it for a CDN library — no third party in the chain IS the app.** Slim shell + 8 `source/` modules, all under the line. **🔴 THE CORRECTNESS BAR IS A REAL SCANNER.** `qr-selftest.js` proves the arithmetic, both BCH strings and a codeword read-back out of the finished matrix; **it CANNOT prove a printed code reads and nothing in a tab can.** Any encoder edit runs the self test AND gets scanned off paper. **📐 MODULE SIZE IN mm, NOT PIXELS, DECIDES WHETHER A CODE SCANS** — takes a physical width, flags <0.5mm (marginal on labels/thermal) and <0.33mm (below phone-camera resolution); any sizing/export change keeps that readout honest. ECC defaults **Q**, spare room auto-raises it. **🔴 v1.1 — `[hidden] { display: none !important }` IN `app.css` IS LOAD-BEARING. DO NOT REMOVE IT OR STRIP THE `!important`.** `hidden` hides ONLY via the UA rule at the bottom of the cascade, so any class setting `display` beats it: v1.0 shipped `.cover{display:grid}` and the access-gate screen covered the app **on every load while `config.json` said `open`** — the JSON looked like it was lying. **Five elements were broken at once** (`#cover`, `#cover-form`, `#drawer`, `#p-batch`, `#readout`, `#acts` — all flex/grid), and the DOM reads as CORRECT because the attribute is right there doing nothing. Every `hidden` toggle in `app.js`/`batch.js` depends on that one rule. **⚠️ The `?v=` bump is load-bearing here too** — a cached v1.0 stylesheet re-breaks a fixed build. 🔎 **It was missed because the v1.0 ship check fetched `config.json`, never a rendered page: a verify that cannot see first paint is not a verify.** ⚠️ **NO `shared/themes` JOIN — a DELIBERATE v1 call**: the join's two traps (JOIN vs COLOR slug on `applyTheme()`, blank vector cell = silent half theme) against a build whose budget belonged to the encoder. Local tokens carry a labelled fallback floor and **every spacing `var()` has a fallback** — `template-app`'s bare-`var()` ramp silently zeroes padding and this app does not inherit it. ⚠️ `og.png`/`icon.png` referenced but **not committed** (binaries → UI drop); unfurl + install degrade, not break. 🚫 **No inverted/dark-background code option** — scanners need dark-on-light. 🅿️ **HEADLINE NEXT BUILD: a self-hosted `r/` redirect tree** so a code stays permanently static while its destination stays editable, the answer to the one real weakness of static codes. **Open questions parked in `next-build-spec.md`; do not build it before Michael answers them.** |
| `report-normalizer` | ⚠️ **unverified, never indexed** | Found 2026-07-25, absent from BOTH former indexes. Needs a version stamp + a real row on next touch. |
| `retrocast` | ⚠️ **unverified, never indexed** | Found 2026-07-25 — despite being the **FIRST theme-spine consumer** per this ledger's own rollout note. Needs a version stamp. |
| `routines` | 🗑️ **DELETED 2026-07-27 — NO LONGER AN APP** | PR #562. Folder survives as **docs only**, listed so the app is not re-added. **Do not rebuild it.** Why it died + the generalising lesson: `routines/next-build-spec.md`. |
| `squash` | ⚠️ **unverified, never indexed** | Found 2026-07-25. Needs a version stamp + a real row. |
| `Vectorworks` | ⚠️ **unverified, never indexed** | Found 2026-07-25. Also the ONLY capitalized folder in a kebab-case repo — needs a naming decision alongside its stamp. |
| `world-cup-bracket` | shell v5.4 · data 2026-07-07 | PRs #76/#77. Data-separated. **⚠️ Its refresh ROUTINE is retired (tournament ended Jul 19) — the APP is not. Still live, do not retire it.** |
| `f1-results` | ⚠️ **NOT AN APP** (tombstone) | Kept so the phantom isn't re-added: this is f1-racetracks' **nested data store**, never a root app. |
| `filemaker` | unverified (reference) | FileMaker app docs; runs in FMP, not on Pages. Verify on next touch. |

**Non-app infra (never PR as an app):** `brain-config`, `agent-reports`, `shared`, `quickfire`, `template-app`.

- **`template-app` — gold standard, v5 (PR #300). Access: GATED, code `0426`.** The copy/place/audit baseline: slim hash-router `index.html` + `chrome.js` + `pages/` partials, styled off the `shared/themes` spine. Carries `CONFORMANCE.md` as the audit checklist. ⚠️ **Its `chrome.js` (10,617 B) is the FULL app chrome**, with **no embed/iframe awareness**. ⚠️ **A COPY BASELINE, not a shared runtime: every copy is a fork, so a fix in one reaches no other app.** Place it UNFORKED; if it cannot express what you need, **change the template** — Michael's call. 🐞 **THREE OPEN DEFECTS (08-03, all one-line, all Michael's call):** (1) `styles.css` — a literal `*/` inside a CSS comment ends it early and leaks the rest into the stylesheet; (2) `CONFORMANCE.md` names `default-theme` as a JOIN when it is a COLOUR, so following it literally faults; (3) 🔴 **the `--s1`…`--s8` ramp uses bare `var()` with no fallbacks — one unresolved token silently zeroes every page and header gutter in every app copied from it** (an unresolved `var()` invalidates the whole declaration; `padding` reverts to `0`, so it vanishes rather than degrading). 🔎 **UNVERIFIED LEAD (PR #848):** check whether `chrome.js`/`styles.css` toggle `hidden` on anything the stylesheet gives a `display`, and whether a `[hidden]` rule exists — if not, every copy carries `qr-forge`'s latent defect. Narrative → PR #952. |
- **`shared/themes` — the 4-vector theme spine.** A THEME is a JOIN in `_themes.json` binding one row each from `colors.tsv` / `typography.tsv` / `forms.tsv` / `spacing.tsv`. 🔴 **`applyTheme()` takes a JOIN slug; the `data-theme` DOM attribute takes a COLOR slug.** Passing a colour to `applyTheme()` faults — it bit `inciardi-collection` and `f1-racetracks`, and `git-grab` nearly shipped it because its own reviewed plan named a colour. Since PR #502 an unresolved pointer paints a red banner instead of half-theming in silence. **🔴 HEX IN THE TSVs IS CANONICAL — `resolve.js` reads ONLY the four TSVs + `_themes.json`**, so `paddock.json` and `f1/*.json` are PROJECTIONS; on a disagreement the TSV wins. ⚠️ **`themes.css` is GENERATED, last built 2026-07-16, so it has NO blocks for `carbon`, `eos` or `paddock`** — those joins MUST resolve at runtime. **ADDITIVE — a new row reaches nobody until a join points at it.** 🐞 **`applyVector()` faults on a missing ROW but never on a BLANK CELL**, so a half-filled vector row applies a half theme in silence, inside the file whose header is a manifesto against silent fallbacks. Change history + the unresolved token-count disagreement → PR #952. |

**Coverage rule:** every folder in the repo root is either in the table above or on the not-apps line. 🔴 **BREACHED, and NOT by a clipped read: `audio-compressor`, `doc-sandbox` and `agent-load` were all live in the root and absent from this table until 2026-09-21** — three apps built-but-unindexed. **An app nobody indexes is an app nobody verifies** — and a listed row is not a verified row. Reasoning + the verification log: [the standard](brain-config/ledger-standard.md).
